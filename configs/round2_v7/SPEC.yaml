version: round2-v7-periodic-networks
date: 2026-09-26
platform_target: 96.4
platform_reference: {candidate: V6_PORT_TIME_A35, score: 96.3366, source: user_reported}
data: {snapshot: round2_v2, external_data: forbidden, pretrained_weights: forbidden}
split_seeds: [42, 3407]
folds: 5
confirmation_seeds: [7777, 12011]
reference_by_target: {tap_iron: A35_IRON, tap_time_len: A35_TIME}
reference_time_alpha: 0.35
secondary_reference_time_alpha: 0.20
recipes:
  mlp_raw: {backbone: mlp, frequency: null}
  mlp_plr001: {backbone: mlp, frequency: 0.01}
  mlp_plr01: {backbone: mlp, frequency: 0.1}
  tabm_raw: {backbone: tabm, frequency: null}
  tabm_plr001: {backbone: tabm, frequency: 0.01}
  tabm_plr01: {backbone: tabm, frequency: 0.1}
candidates:
  tap_iron: [mlp_raw, mlp_plr001, mlp_plr01, tabm_raw, tabm_plr001, tabm_plr01]
  tap_time_len: [mlp_raw, mlp_plr001, mlp_plr01, tabm_raw, tabm_plr001, tabm_plr01]
tie_preference_by_target:
  tap_iron: [mlp_raw, mlp_plr001, mlp_plr01, tabm_raw, tabm_plr001, tabm_plr01]
  tap_time_len: [mlp_raw, mlp_plr001, mlp_plr01, tabm_raw, tabm_plr001, tabm_plr01]
training:
  random_seed: 42
  inner_seed: 42
  width: 256
  blocks: 2
  tabm_k: 16
  dropout: 0.1
  embedding_dim: 16
  n_frequencies: 16
  lite: true
  loss: mse
  optimizer: adamw
  learning_rate: 0.001
  weight_decay: 0.0001
  batch_size: 256
  max_epochs: 240
  patience: 25
  min_delta: 0.00001
  early_stop_metric: standardized_mae_of_ensemble_prediction
  preprocessing: inner_training_only_then_refit_outer_training
  refit: fresh_initialization_full_outer_training_at_selected_epoch
blend_grid: [0.0, 0.05, 0.1, 0.2, 0.35, 0.5, 0.75, 1.0]
budget:
  development_outer_fits: 120
  optimizer_runs_per_outer_fit: 2
  workers: 8
  confirmation_candidates_max: 2
  confirmation_outer_fits_max: 20
  full_data_fits: 0
confirmation_admission:
  require_two_positive_complete_development_seeds: true
  order: mean_increment_then_frozen_cost_order
promotion:
  min_split_seeds: 4
  seed_level_paired_lcb95_gt: 0.0
  all_seed_gains_positive: true
  fold_level: descriptive_only
  local_working_gate: 96.25
release: {automatic_packages: false, agent_uploads: 0}
