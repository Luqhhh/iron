# Local M2/M3/M4 and real OOF implementation plan

**Goal:** complete local optimization_v02 candidates and real training-set OOF.
**Spec:** spec.md, with user approval on 2026-09-08 for real OOF and local-only M4.
**Architecture:** shared frozen-parameter dual-target estimator; independent M2 weights and M3 causal anchors; prediction-frame M4. One as-of builder and one protected-development loader feed every fold.
**Tech stack:** locked Python 3.12, CatBoost 1.2.8, pandas and pytest.
**Execution:** inline using executing-plans and TDD. No commits/push; keep current branch and workspace per user preference.

## Contracts

- Keep baseline-v0.1 source/configuration/parameters immutable.
- Development reads use read_development_labels/history, strictly before 2024-11-01.
- Use the existing three June/July/August one-month folds and pooled two-target WMAPE E.
- M2: weight = 2 ** (-age_days / half_life_days), normalized to mean one; fixed grid 30/60/120.
- M3: per-row expanding B1 (min count 20), global median fallback. Use only earlier reference times with labels available by that row. No-history rows are excluded from residual fitting, with counts and identity recorded. Evaluation anchor freezes eligible training labels at fit cutoff.
- M4: local M1/M2/M3 only; require at least two candidates passing existing OOF stability gates, residual correlation below .95 in at least one target and an equal-weight probe improving over the better member. Then select per-target convex pair weights on [.25,.5,.75]. Otherwise record SKIPPED, not model failure.
- M1/M2/M4 tuning uses OOF only. Report selected OOF metrics as selection evidence, not independent generalization.
- Freeze selection before DEV_LONG/SHORT confirmation. Both confirmatory folds overlap earlier developmental observations; report this limitation.
- G1 uses spec.md gates; significant worst-fold regressions require review. Report G0 independently.
- Save separate candidate bundles with model, anchor, feature schema and component digests; verify prediction round-trip. Retain all failed runs.

## Tasks

- [x] M2: tests for 2:1 decay ratio, mean normalization, future-row refusal and real estimator bundle round-trip. Implement recency_weights and frozen-parameter weighted estimator.
- [x] M3: tests for own/future/unavailable label invariance, per-spout fallback, empty-history warm-up, signed residual fitting and restored predictions. Implement expanding_anchors and ResidualModel.
- [x] M4: tests for missing/unstable inputs, shuffled IDs, complementary residuals and frozen weights. Implement quality checks and select_ensemble with explicit SKIPPED evidence.
- [x] Real-data runner: protected loader, frozen configuration digests, metadata availability joins, one feature build per fold, B0/B1/CatBoost/M2/M3 predictions, row-level audit, bundles and non-overwriting run state. Test with November sentinel labels before executing.
- [x] Selection: reuse M1 selection, choose M2 half-life, apply M4 gate, write immutable selection snapshot. Compute pooled and fold metrics with both targets and error slices.
- [x] Run real rolling OOF; verify source identities, protected ledger, bundles and determinism. Run DEV_LONG/SHORT after frozen selection, with no retuning.
- [x] Run locked Python 3.12 suite and private artifact guard; document G0/G1 and paths.

## Verification commands

Use UV_PROJECT_ENVIRONMENT=/tmp/iron-optimization-v02-py312 with uv run --frozen --extra dev --python /home/clairvoyant/.local/share/uv/python/cpython-3.12.13-linux-x86_64-gnu/bin/python3.12.
Run pytest tests/optimization_v02 first, then pytest -q and python scripts/check_no_private_artifacts.py.
Numerical tests use literal expected ratios/anchors. Integration tests exercise real CatBoost with frozen 800 iterations on small data.
