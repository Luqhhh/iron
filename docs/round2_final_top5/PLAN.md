# 全部已验证离线候选 Top 5 最终交付

用户明确要求从全部已验证离线候选中选Top5并生成提交包。新规则为按两组完整OOF的综合本地分数排序，所有包仍相对B基准仅替换一列。此次独立交付允许尚未封包的V2.6方案入列，不回写V2.6当时“未超过DJ，不晋级”的决定，也不宣称这些方案优于DJ。此前五包和历史排程全部保留。

汇总V2及V2.1–V2.10、V2.12已验证指标（V2.11仅诊断），按目标/候选去重；同名重复指标必须一致。时长未改时用完整B3指标，铁量未改时用C2指标，不任意组合两个新目标，不恢复已移出的T1。分数为100*(1-J)，不估计平台分数；不把名次细微差异当显著差异。

冻结Top5：DJ、J3、DJ3、AJ3、AJ，全部铁量替换，时长复制B包原字符串。DJ=.5C2+.25D4+.25J1_42；J3=J1_42/2026/2027三成员平均；AJ3=.5C2+.5J3；DJ3=.5I2+.5AJ3；AJ=.5C2+.5J1_42。按原嵌套运算顺序生成，模型种子顺序42、2026、2027。未改变旧配置或比例。

预算新增CV0、全量2个JointSnapshotRegressor（种子2026、2027），三个新包共用这两个新模型；种子42原全量模型复用。DJ、AJ直接复制原ZIP和CSV并核对冷推理重建字节一致。旧模型、数据、折和原包摘要全保留。

先复算Top5 OOF并只读重放V2.6全部同折成员，再训练两模型。发布校验：完整322个官方ID、三列、根目录仅result.csv、非负有限、反序/分批/单样本一致、未改时长字符串一致、五包不重复、冷进程禁止训练标签读取且CSV逐字节一致。温进程的全量模型身份/目标尺度复核可读取V2训练标签，明确与冷进程区别。685项既有测试基础上新增定向测试并执行完整锁定Python3.12回归。

最终写入桌面submission/final-top5/，每个编号子目录一个单独上传ZIP，附本地排名与SHA清单。目录已有时拒绝覆盖。所有包仍由用户自行上传，未上传或独立登录核验平台分数。
