# Round2 V6 结果：Stage A 零拟合筛查（先给出否定结论，并暴露一处预登记设计缺陷）

日期：2026-09-26  
分支：`round2-v6-iron-capacity-networks`  
状态：**STAGE A 与铁量容量探针均已执行，结论为否定：Stage A 两个目标 0 个准入；探针（`raw_tabm|large`、`ple_tabm|large`）未过探针门槛 → 铁量新容量空间不再筛查。未执行 Stage B/C/D，未封包，未上传。新增拟合共 4 个 slot。**

## 1. Stage A 结果

以已记录的 V3.6 folds 0/1 预测（seed 42）对**释放列**做签名筛查（`ρ ≤ 0.95`、精度比 `≤ 1.25`、嵌套 2 折投影增益 `> 0`）：

| 目标 | 参与筛查 | 准入 | 选中 |
|---|---:|---:|---:|
| `tap_iron` | 12 | **0** | 0 |
| `tap_time_len` | 38 | **0** | 0 |

排除的 14 个试次是已具备完整覆盖者（铁量 `ple_mlp|small` 4 个 + 时长 `raw_tabm|large` 4 个 + V5 补全的同类 4 个计数重复，见 `stage_a.json` 的 `excluded_complete_trials`）。

### 1.1 失败模式（为什么全灭）

| 目标 | `accuracy_loss_above_max` | `projected_gain_not_positive` |
|---|---:|---:|
| `tap_iron` | 9 / 12 | 12 / 12 |
| `tap_time_len` | 25 / 38 | 38 / 38 |

- **铁量列**：可筛的 12 个试次**全是 `small` 容量**，精度比 1.15–3.03（`ple_tabm|small` 最差，3.03），投影增益全为负（最好 −0.0027）。
- **时长列**：`ple_mlp|medium/large` 的 ρ 0.847–0.858、精度比 1.14–1.18，**相关性与精度其实过关**，但**投影增益全为负**（最好 −0.0003）。
- 结构性结论：**ρ 与精度比是必要不充分条件**——真正的判别量是“混合是否有增益”，而在这个未补全的空间里没有任何一个族能通过它。

### 1.2 筛查装置自检（这一步很关键）

同一套代码对**已被 V5 验证的四个获胜成员**（从 recorded folds01 文件判读）给出：

| 试次 | ρ | 精度比 | 投影增益 | 准入 |
|---|---:|---:|---:|:--:|
| `v36-s1-N-0049` | 0.8758 | 1.060 | +0.02009 | ✅ |
| `v36-s1-N-0050` | 0.8821 | 1.064 | +0.01299 | ✅ |
| `v36-s1-N-0048` | 0.8875 | 1.060 | +0.01144 | ✅ |
| `v36-s1-N-0051` | 0.8740 | 1.068 | +0.01131 | ✅ |

并且 recorded folds01 预测与 V5 补全后的 seed-42 向量在这些行上**逐位相同（max diff = 0）**。所以 Stage A 的否定结论不是装置失灵，而是对**已记录的那部分空间**的真实结论。该自检已固化为回归测试 `test_screen_reproduces_the_known_winner_as_admissible`。

## 2. 铁量容量探针（用户授权的 2 试次 / 4 slot）

Stage A 暴露了预登记的规则适用范围缺陷后，用户选择最小探针（选项 A）。为此把 SPEC 修订到 `revision 3`：**stop rule 的来源范围被纠正**——Stage A 只约束已记录来源（S1），而铁量新容量空间（构造上无已记录预测）由其自身的探针把关。未改动任何阈值。

探针对象（获胜成员的直接类比，`--stage v6-probe` 默认集合）：

| 试次 | 族 | folds 0/1 池化 WMAPE | 精度比 | ρ | **投影增益** | 准入 |
|---|---|---:|---:|---:|---:|:--:|
| `v6-s1-N-0024` | `raw_tabm\|large` `mse_adam` | 0.039951 | **1.095** | **0.8957** | **−0.0014** | ❌ |
| `v6-s1-N-0028` | `ple_tabm\|large` `mse_adam` | 0.138513 | 3.795 | 0.332 | −0.0522 | ❌ |

（铁量基座 folds 0/1 池化 WMAPE = 0.036498。）

**关键点：`N-0024` 的边际签名几乎与获胜成员相同，却在绑定判据上失败。**

| | 获胜成员 `v36-s1-N-0048`（时长） | 探针 `v6-s1-N-0024`（铁量） |
|---|---:|---:|
| ρ | 0.888 | 0.896 |
| 精度比 | 1.060 | 1.095 |
| 逐方向最优 α（fit 0 / fit 1） | **0.20 / 0.21（一致）** | **0.18 / 0.05（分岔）** |
| 逐方向增益符号 | 同为正 | **一正一负** |
| 投影增益 | +0.0114 | **−0.0014** |

### 2.1 为什么失败——细粒度 α 曲线（诊断，非放宽阈值）

| α | fit1→eval0 增益 | fit0→eval1 增益 | 均值 |
|---:|---:|---:|---:|
| 0.005 | +0.00038 | +0.00003 | +0.00021 |
| 0.020 | +0.00154 | +0.00001 | +0.00078 |
| 0.050 | +0.00349 | −0.00038 | +0.00155 |
| 0.120 | +0.00613 | −0.00262 | +0.00176 |
| 0.200 | +0.00678 | **−0.00784** | **−0.00053** |
| 0.300 | +0.00414 | −0.01788 | −0.00687 |

**两个方向符号相反、随 α 单调分岔**——这是不稳定，不是增益。因此失败是**实质性的**，不是网格下界造成的假阴性（把 α 降到 0.03 也只有 +0.0011，且仍是一正一负）。

### 2.1.1 两次被我自己推翻的归因（记录在案）

**第一次猜测（行胜率）**：我把失败归因于“候选在多数行上更差（41.7% 行更准、MAE 更大）”。按同口径测获胜成员后**被推翻**：

| 候选 | 行上更准比例 | 平均绝对误差（候选 / 基座） | ρ |
|---|---:|---|---:|
| **获胜** `N-0048`（时长） | **47.5%** | 5.107 / 4.819 | 0.888 |
| **探针** `N-0024`（铁量） | **41.7%** | 21.312 / 19.470 | 0.896 |

获胜成员同样“在多数行上更差、MAE 更大”，却依然有效。行胜率与 MAE 不是判别量。

**第二次猜测（α 稳定性）——同样被推翻，且这次是我自己的方法错误。** 我在 §2.1 里把获胜成员的逐方向 α `0.20/0.21` 与探针的 `0.18/0.05` 对比，得出“α 稳定性是判别量”。但那两个数字**来自不同样本量**：

- `0.20/0.21` 是 V5 在 **2 seed × 5 折 = 10 格**上的嵌套结果；
- `0.18/0.05` 是探针在 **folds 0/1**上的结果。

在**同一口径（folds 0/1，seed 42）**下重算 14 个见证对象后，结论反转：

| | Δα | 逐方向增益符号 | 均值 |
|---|---:|---|---:|
| 获胜 `N-0048` | **0.22** | **一正一负** | +0.0114 |
| `N-0049` | 0.15 | 同为正 | +0.0201 |
| `N-0050` | 0.20 | 一正一负 | +0.0130 |
| `N-0051` | 0.18 | 一正一负 | +0.0113 |
| 探针 `N-0024` | 0.13 | 一正一负 | −0.0014 |

已知好集合的 `Δα`（0.15–0.22）**比已知坏集合（0.00–0.13）更大**：该统计量不仅不能判别，排序还是反的。原因是 folds 0/1 只有 551 行/折，单人折的 α 估计极不稳定——同一个获胜成员在五个折上分别是 **0.38 / 0.16 / 0.29 / 0.12 / 0.28**，随样本增大才收敛（2 折 0.31、3 折 0.30、5 折 0.24、10 格 0.20/0.21）。

**因此：(a) “逐方向 α 稳定性”作为判别量不成立，已撤回；(b) 更重要的是，folds 0/1 这个筛查样本本身太噪，不足以支撑任何逐方向排序——V5/V6 的选候选都建立在这个噪声筛查之上。**

### 2.2 本轮最重要的方法结论

**ρ、精度比、行胜率、MAE、逐方向 α 稳定性，全部不是有效签名。** 14 个见证对象（含 1 个平台验证获胜者、6 个完整覆盖的时长成员、4 个 V3.6 铁量成员、2 个铁量探针）上唯一**排序正确**的量，是**朴素的平均嵌套增益**（folds 0/1）：

| 族 | 平均嵌套增益（分） |
|---|---:|
| 时长 `raw_tabm\|large`（获胜族，4 个近似重复） | +0.0113 ~ +0.0201 |
| 时长 `raw_tabm\|small/medium` | +0.0010 ~ +0.0041 |
| 铁量 `ple_mlp\|small`（V3.6 已精炼） | −0.0007 ~ −0.0019 |
| 铁量探针 `raw_tabm\|large` | −0.0014 |
| 铁量探针 `ple_tabm\|large` | −0.0522 |

这个判据正是 V5/V6 已经冻结使用的那个。因此 **V6 预登记的签名没有欠定义——“改用新签名重筛”这一设想不成立**（见 §5 的修正）。

真正的问题不是签名，而是**筛查样本**：folds 0/1（1102 行、单 seed）连获胜者都判不稳，却承担了全部选候选决策。

→ 下一轮若继续，签名必须改为**混合性质本身**（逐方向 α 稳定 + 两方向同号 + 小 α 增益），而不是 ρ/精度比/行胜率。这同时解释了 §1 里时长列 `ple_mlp` 族的失败：它们 ρ 与精度比都过关，投影增益为负。

### 2.2.1 追加的零成本证伪：没有第二个成员能叠加在获胜者之上

方向 D 的另一半设想是“找一个结构不同、与获胜者互补的成员叠加上去”。我用**留一折权重拟合**（在 9 折上选权重、在第 10 折上评分，2 个已记录 seed × 5 折）直接检验了这一点：

| 组合 | 留一折均值增益 | sd | 正折 | 相对 2 成员 |
|---|---:|---:|---:|---:|
| `base + N-0048`（2 成员） | +0.00692 | 0.01447 | 7/10 | — |
| `+ N-0040` / `0041` / `0042` / `0044` / `0045` / `0046`（逐个） | +0.00692 | 0.01447 | 7/10 | **+0.00000** |

六个结构不同的时长成员（`raw_tabm|small`、`raw_tabm|medium`、`mlp` 族）**全部被权重优化归零，增益恰好为 0.00000**。因此“第二个成员叠加”这条路在已记录的时长列上**不存在**。

### 2.2.2 真正的流程缺陷：筛查样本太噪

同一批分析暴露了比签名更要紧的问题：获胜成员 `N-0048` 在**单折**上估出的最优 α 分别是 **0.38 / 0.16 / 0.29 / 0.12 / 0.28**，随样本增大才收敛（2 折 0.31 → 5 折 0.24 → 10 格 0.20/0.21）。也就是说，**folds 0/1（1102 行、单 seed）连获胜者都判不稳**，而 V5 与 V6 的**全部选候选决策**都建立在这个筛查之上。任何后续搜索轮必须把筛查搬到**完整覆盖**（≥5 折 ×2 seed）上，而不是折叠成 2 折省算力。

### 2.3 对"铁量容量杠杆"假设的判定

探针是该假设**最有利的单点检验**（获胜结构的直接类比、最佳损失设置），它失败了。因此：

- 按预登记探针门槛，**铁量新容量空间不再筛查**；
- 现状保持平台最佳 `V5_TIME_N0048_Q20 = 96.3143` 不变；
- “容量杠杆可跨目标复制”这一说法**未获支持**，且失败原因已被定位（边际相关性 ≠ 互补性）。

## 3. 本轮暴露的预登记设计缺陷（必须登记）

预登记写下的 stop rule 1 是：

> “若两个目标都没有准入候选，则停止并报告；**不拟合铁量新容量空间**。”

而本轮唯一想要检验的对象（铁量 `medium`/`large` 网络）**在构造上不可能出现在 Stage A 里**——它没有任何已记录的 folds 0/1 预测，正是 `stage_a2_iron_capacity_screen` 这条预算存在的理由。于是：

- Stage A 只能证明**已记录的小容量空间没有可捡的东西**；
- 它对**容量杠杆**（V5 在时长列唯一有效的机制）几乎不提供信息；
- 而 stop rule 1 却用它把容量杠杆挡在门外。

这是**规则与其自身目的不一致**，不是阈值松紧问题。按预登记纪律，我**没有**自行绕过它：本轮到此停止，`stage_a2_unlocked = false`。是否修正该规则的适用范围属于用户决策（见第 4 节）。

## 4. 本轮的花费与产物

| 项 | 值 |
|---|---|
| 新增拟合 | **0** |
| Stage B/C/D | 未执行 |
| 封包 / 上传 | 0 / 0 |
| 预登记修订 | `configs/round2_v6/SPEC.yaml` 的 `amendment: revision 2`（新增 fusion α 网格、S2 粗筛预算、reference 路径；**未改动任何阈值/门槛/上限**） |
| 私有证据 | `local/runs/round2-v6-iron-capacity-networks/stage-a-r1/stage_a.{json,csv}` |
| 测试 | `tests/test_round2_v6.py` 10 项（含装置自检与去重约束） |

## 5. 后续选项（D 的前提已被本轮校准推翻，下面是修正版）

用户选择的方向 D（“用新签名重新预登记并复筛时长列剩余族”）建立在“α 稳定性是判别量”之上，而 §2.1.1 已用 14 个见证对象证明该前提不成立。因此把 D 修正为**仍然零/低成本、且有真实信息量**的两件事：

- **D′-1（推荐先做，成本最低）**：**把噪声筛查换掉**。对**已具备完整覆盖**的 6 个时长小/中容量成员（`N-0040/0041/0042/0044/0045/0046`，folds 0/1 上均值为 +0.001~+0.003，其中 `N-0044` 在 10 格上为 +0.0033、ρ=0.860，与获胜族**不同结构**）在派生 seed `7777`/`12011` 上补四 seed 复现（**基座已缓存，只需候选拟合** ≈ 10 折）。这直接回答一个尚未回答的问题：**在本地幅度被压缩约 4 倍的前提下，一个“小而稳”的独立成员能否在平台上再加一层？**
- **D′-2（若要更大范围）**：给**未被精炼过的结构族**补完整覆盖（每族只取最佳训练设置，如 `raw_mlp|large`、`ple_mlp|large`、`ple_tabm|large`、`raw_tabm|medium` 各 1 个 × 2 seed × 5 折），因为 folds 0/1 筛查把它们判为“≈0”，而该筛查已被证明不可信。
- **D′-3（1 次额度，直接）**：对已验证的 `V5_TIME_N0048_Q20` 做一次 **α 上探**（如 α=0.30）：本地曲线在 0.20–0.25 几乎持平（0.038164 vs 0.038175），而平台对本地幅度有 4.19× 放大，**方向未知**，属 1 次额度可判定的实验。
- **D′-1（补充证伪，已执行）**：见 §2.2.1——六个结构不同的时长成员对获胜者包的增益**恰好为 0**，该路径关闭。
- **E/F（原选项不变）**：转向全新模型族，或停在 `96.3143`；若还要动一次额度，只有 D′-3（α 上探）·且**先验略偏负**（本地 α=0.25/0.30 分别差 0.0005/0.0022 分），属方向未知的一次实验。

## 6. 声明

- 未执行拟合、未封包、未上传；`local/` 私有产物无入库。
- 预登记修订在**执行前**落盘并在此逐条说明，只增加可执行细节，未放松任何阈值、门槛、停止规则或预算上限。
