# optimization-v0.11 / OPT-24 frozen execution plan

Authorized 2026-09-11 from optimization-v0.10 commit a7bf539c07dc98d59b5c2432c1a60694c52f1fd1. Only candidate V5_TIME_TRAJECTORY. November is consumed; all evaluation is retrospective and not independent holdout or platform confirmation. Current release is V1 (user-reported 83.0319), fallback R2 (83.0207). V2/V3/V4 remain closed.

P0 uses zero supervised fits. Freeze source/configuration/data/reused artifact digests and append access ledger before training labels or scoring labels. Verify April–November original OR/HR/rate bundles, cutoff, training identity, schema, model hashes; reproduce original R2/V1 saved forecasts. Missing artifacts block; restore originals, never retrain replacements. Check existing feature definitions and all cutoff feature coverage, constants, finite values, time spans and adjacent-pair counts without evaluation labels. An already-existing whole feature group or inability to construct trajectories blocks, with no alternative family.

Independent feature profile appends exactly 24 columns to the original E09/R2 matrix: air_volume, total_press_diff, hot_air_press, oxygen × 6h,24h × slope,mean_abs_step_rate,valid_pair_count. Preserve every original column, dtype and value. Use the existing duplicate/conflict contract and sorted event times. Visible events satisfy event_time <= reference_time and available_at <= reference_time; windows are (reference_time-W,reference_time]. Slope is closed-form OLS using real hours and at least three distinct finite observation times. Step rate averages abs(dx)/dt only between adjacent original observations with finite endpoints and 0<dt<=1.5h; at least two pairs for the signal; always retain actual count. Missing observations break adjacency; no interpolation, future fill or zero imputation. Timing contract remains competition-timestamp-contract-v1 ASSUMED. No new libraries.

Eight development single-target CatBoost fits, April–November, original E09/R2 endpoint training IDs/order/history and configs/baseline.yaml parameters (MAE, 800 rounds, depth 5, learning rate .03, L2 5, seed 2026). No early stopping or weights changes. Zero new iron/E04/rate/q/residual fits. Six separate scalar LAD fits, June–November, only earlier monthly causal OOF whose labels are available at each outer cutoff. March initializes training; April/May initialize June LAD. Same minimum 100 OOF rows and original LAD tie/zero rules. An origin's coefficient stays fixed across all horizons.

Clip each original component as before. New base time = .8*T09-new + .2*T04. Direction = I_R2/R - new base time when original rate >1e-6, else zero. V5 time = max(0,new base time+alpha_T*direction). V5 iron is an exact array copy of original V1; never feed new time into V1 iron correction. Keep old time model for that path. Track rate fallback counts.

Freeze all outer predictions before scoring-label access. Same six H1 origins, 18 cells and DEV_LONG/SHORT. E=.5*(iron WMAPE+time WMAPE). J averages origins within each horizon then equally averages four horizons. Deltas V5 minus V1. All gates required: exact iron; delta J<=-.0005; H1 mean time delta<=-.0010; at least four strictly improved H1 E origins and two of Sep/Oct/Nov; each H2/H3/H4 mean E delta<=+.0005; each DEV E delta<=+.0007; all engineering/causal checks. No supplemental selection rules or partial submissions. Failure retains V1 and closes this candidate.

Diagnostics only: per-target/spout/month WMAPE, bias, numerator/denominator; base-time versus corrected paired changes and prediction-delta quantiles. Base-time diagnostic is not another candidate. Shared calendar-week paired bootstrap, 1000 draws seed 2026, shared weights for repeated samples and recomputed denominators. Consumed-data stability only, no expected platform score inference.

Checks: future append invariance, ordering and duplicates/conflicts, missing/constant/long-gap/boundary synthetic data, same old aggregates with different internal order, old matrix exact preservation, OOF provenance, input reversal, persistence and cold-process reproduction at existing tolerance, zero inference fits, cold inputs exclude official target paths and histories restored from bundles. Rerun locked Python 3.12 suite, report G0 separately from G1. Preserve failed evidence, historical runs, original bundles, ZIPs and ledgers.

Only all-gates pass permits one final time CatBoost fit plus one final time LAD, original final R2 identity of 2754 rows at 2024-12-01 01:44:00+08:00, April–November OOF labels available by cutoff. At most one new named bundle/ZIP: 335 test_a rows, exact ID/order/schema, finite nonnegative values, six-decimal CSV/ZIP reread, cold verification and iron equality. Mark READY_CHALLENGER, retain active V1 pointer. No automatic upload or desktop overwrite; manual platform last/best-submission rules must be confirmed before a trial bound to the ZIP hash. No retry based on platform score.

Deliver local manifest, feature audit, OOF provenance, acceptance, cold validation, fit counts, consumed status and repository RESULTS even if blocked or rejected. No training, release or ledger framework replacement; reuse existing components and evaluation.
