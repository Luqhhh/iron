# V2.11 当前最佳模型残差结构审查

前轮20次Depthwise拟合提供负面证据：训练误差更低、验证更差。本轮先判断已有输入中是否存在持续的系统偏差，避免继续无依据增加容量。只使用已冻结DJ铁量/AORD时长OOF与V2公开训练输入/标签，不训练、不读取测试标签、不调整测试预测。

对21个原数值输入及四个预先定义的组合做10分位组诊断：oxygen/(60*air_volume)、total_press_diff/air_volume、hot_air_temp-furnace_throat_temp、upper_press_diff/total_press_diff。分母必须为正，异常时报错，不裁剪或插补。前两项及上压差占比只是数值组合，不宣称满足真实物理方程；字典温度单位一致，富氧与风量时间单位转换明确。

按全训练输入确定的分组仅用于描述，不进入推理或模型训练。每组报告样本数、两切分平均有符号残差（预测减真值）、平均绝对误差、两切分偏差同号、铁口分布。另报告每个特征与两组平均OOF残差的Spearman相关，以及各组平均偏差绝对值的加权平均。后者是同批开发OOF上的乐观描述，不是可实现的校正收益、显著性检验或独立验证；不把两个切分的同一样本当两条观测，不使用分组做样本删减/加权/人工偏置。

本次新增拟合0，候选包0；原五包队列不变。冻结数据、折、已保存OOF、源码与旧包摘要，独立进程复核计算。若发现系统偏差，只作为下一轮固定特征工程方案的依据，不能把分组校正直接用于隐藏测试。
