# optimization-v0.23：恢复与完整算法对照

本阶段不是新的 V23 预测候选。它固定在
`3524fcbb0455092c868f1fa22f217084732059ff`，只完成三件事：恢复并验证历史
V21 payload；把 V1、V10、V21_REPLAY、V22 统一为六位预测精度后重算；使用
v0.22 已存的 Q、u、M、lambda 做 `D_M_CUTOFF_ONLY` 诊断。

历史 V21_REPLAY 还必须逐个 June--November origin 使用显式带时区的冻结历史重新
执行，并与各 origin 的存档 CSV 及统一评分输入同时核对。该审计与 test_a 原包恢复
分开记录，防止把单一回放路径的时区问题外推到全部历史评分。

所有模型、预处理器、LAD/lambda 与新中位数拟合预算均为零。复赛入口只准备
V1、V21、V22 三条完整冻结算法；旧 test_b 只作为工程预演，不能登记成正式
复赛数据或质量结果。逐样本预测、恢复包、评分表、报告、账本与冷审计回执只保留
在被忽略的 `local/`。

V21 的平台选择历史、V22 的历史开发 G0/G1 PASS 与 83.1166 初赛回传、以及 V21
的 83.2375 最高用户回传身份分别保留；任何一个事实都不能覆盖另外两个。
