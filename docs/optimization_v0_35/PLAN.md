# v0.35：时长叶点的两项独立聚合实验

状态：已登记实施规格；真实运行、冷审计和封包由同目录 `RESULTS.md` 后续记录。

基点为 `optimization-v0.34-oob-leaf-median-bagging@6eee2e77e0d1393ea1e2f148eb230fa93a54ae4b`。父方案为 V34T（用户回传 83.3201，未取得账号回执）。A 在每棵树的原 v0.29 selected members 上保存中位区间 lower/upper，以 512 个端点的精确有理和得到 256 个中点的等树均值；B 完整复用 v0.34 lower 表，对 256 个树点取排序位置 127 的较小中位数。

两项只改变时长 raw QRF 聚合，均执行原六位 round-trip、v0.15 gate、V21 移动 60 日中位数与 0.25 收缩；铁量逐字符串复制 V34T。A 将 upper 置为 lower、B 将跨树投票切回精确均值时，都必须逐查询及最终 CSV 精确恢复 V34T。A 最终时长不得低于父方案。

预算为零 fit；A 新建 7 份监督中位区间表，B 复用 7 份父 lower 表且不新建 lower 表。两个候选同时冻结、A→B 各一个平台名额，不生成组合或第三候选。平台上传、桌面写入、恢复上传均为 0。私有模型、响应、叶表、预测、账本、回执和 ZIP 不进入 Git。
