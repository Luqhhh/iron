# V2.6：争取替换弱探索包的 J1 固定种子集成

V2.4/V2.5 已有两个正式候选（DJ 铁量、AORD 时长），第三个 BAY 铁量仅为弱探索，其平均增益比用户已移除的 T1 还小。BAY 虽已封包，暂不加入待测。继续一个明确假设：已有 J1 改善铁量，固定三训练种子平均是否减少随机误差，并超过当前 DJ 的离线水平。

保持 MultiRMSE、训练折目标均值变换、depth6/1500/l2=10 不变；训练种子固定42/2026/2027，原seed42折模型和全量模型复用，只新增两个种子。每个样本只平均同一验证折三个模型；不选择最佳单种子。新增20次联合CV拟合；如果新候选通过，则最多新增2次全量联合拟合。时长输出不采用，不作为推理输入。

铁量固定池 J3、AJ3=.5C2+.5J3、DJ3=.5I2+.5AJ3。分类仍对C2，用candidate-tiers-v1；交付额外收窄为只取已选formal且两个切分均超过现有DJ的路线，最多一个。这一附加要求只决定是否值得替换弱探索包，不回写旧门槛。若通过，则本次三个新待测为DJ、AORD和该新包，BAY保留为未排队探索证据。不会把BAY文件删除或伪装成另一包。

这些仍是反复使用的开发折。独立回读验证身份/折/尺度/参数/指标/选择，2000次配对重采样不作门槛；不能保证隐藏测试收益。原V22_I_ONLY及V23_AJ_I_ONLY顺序和内容保留。
