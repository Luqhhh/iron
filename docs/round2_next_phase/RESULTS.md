# Round2 下一阶段：批次 0 与批次 2 增量检验结果

日期：2026-09-24
分支：`codex/round2-next-phase-search`
代码：`src/bf_tap_r2/next_phase_probe.py`、`tests/test_round2_next_phase_probe.py`
计划：`docs/round2_next_phase/PLAN.md`

本文件记录**已实际执行**的结果与由此得出的负证据。运行产物在私有 `local/runs/round2-next-phase/`。

---

## 0. 复现校验（顺带完成）

用冻结配方 `configs/round2_v0_1/models.yaml` 的 **C2**（CatBoost RMSE / depth 6 / iterations 1500 / lr 0.03 / l2 10 / seed 42）在种子 42/3407 五折上重跑。

记录口径说明：`configs/round2_v2/comparison.yaml` 的 `selection` 是 `lowest_two_seed_mean_wmape`，所以历史登记的 C2 数值是**两种子均值**，不是单种子。

| | seed 42 | seed 3407 | **两种子均值** | 历史记录 C2 | 差 |
|---|---:|---:|---:|---:|---:|
| 铁量 WMAPE | 0.04006564 | 0.03977273 | **0.03991918** | 0.03991918 | 0 |
| 时长 WMAPE | 0.04101036 | 0.04049742 | **0.04075389** | 0.04075389 | 0 |

两种子均值的铁量/时长 WMAPE 与 V2 时代登记的 C2 **在 8 位小数上完全一致**；由两者合成的 J 为 0.0403365，与记录的 0.0403365373 也一致。说明本机的数据、折协议与配方能够精确复现历史基线——这是后续一切 delta 可比性的前提。

---

## 1. 批次 0：线性探针

问题：**每个目标的方差有多少是可被线性解释的？** 该答案决定交互特征批次是否有空间。

三种设计，全部只用当前折的训练部分拟合（缩放统计量也从训练部分冻结），预测跨折池化后用冻结的包分公式评分：

| 设计 | seed 42 | seed 3407 | 两种子 |
|---|---:|---:|---:|
| `raw`（21 特征 + 铁口 one-hot） | 91.7620 | 91.7730 | — |
| `four`（+4 个比值特征） | 92.4931 | 92.5048 | — |
| `degree2`（+210 个两两乘积） | 93.5791 | 93.6079 | — |

对照：全局中位数基线 ≈ 82.3；C2 ≈ 95.97；V34_A = 96.1997。

**结论**

1. **"目标方差大部分可被线性解释"这一假设被证伪。** 纯线性 `raw` 的铁量 WMAPE 为 7.45%，C2 为 3.99% —— 误差接近两倍，线性模型比当前最好低 **4.4 分**。先前从单变量相关系数（`oxygen` 0.73）推出的"强主效应已吃掉大部分信号"不成立。
2. **二阶交互确实带信号。** 加入全部两两乘积后 +1.82 分（铁量 WMAPE 相对降 26%，时长降 19%），两个种子、两个目标方向完全一致。
3. **但即使给足全部两两交互，线性模型（93.58）仍远低于 C2（95.97）** —— 存在大量二阶以上的结构。

---

## 2. 批次 2 增量检验：显式交互特征对树模型是否有增量

批次 0 证明的是"**线性模型**需要显式交互"，**不能**推出"**树模型**需要"。V3.4 的 EBM 自带 `interactions` 项，CatBoost 的树分裂本就能表达任意阶交互，因此必须直接测增量。

同一 C2 配方、同一折，只改输入特征：

| 设计 | 两种子均值 铁量 WMAPE | 两种子均值 时长 WMAPE | 包分 |
|---|---:|---:|---:|
| `raw` | 0.039919 | 0.040754 | **95.9663** |
| `degree2`（+210 个两两乘积） | 0.040690 | 0.042335 | **95.8488** |

**结论：显式两两交互特征对树模型是负增量，−0.1175 分，两个目标同时退化。**

树模型已经捕捉了这部分结构；额外 210 个特征只带来稀释。

### 由此产生的负证据（登记在案）

- **盲目铺开全局两两交互特征 → 关闭。** 不要再以"线性模型受益"为由推进该方向。
- 注意本检验**只否证了"全量两两展开"**。计划中允许的"由开发残差定向选出的**少数**交互"未被直接否证，但此结果显著提高了它的举证门槛：任何定向交互都必须先证明相对 `raw` C2 有增量。

---

## 3. 对计划的影响

| 批次 | 原计划 | 调整后 |
|---|---|---|
| 批次 1（时长侧 EBM 残差） | 待定 | **提升为唯一主线**。线性地板 91.76 与当前最好 96.20 之间的 4.4 分全部来自非线性建模，残差/EBM 专家有真实空间 |
| 批次 2（交互特征） | 与分铁口收缩同批检验 | **交互特征一侧降级**（全量展开已关闭）；分铁口收缩一侧保留，仍需按原标准举证 |
| 批次 3、4 | 不变 | 不变 |

---

## 4. 复现方式

```bash
uv run --extra round2 python -m bf_tap_r2.next_phase_probe --mode linear   --root . --seeds 42 3407
uv run --extra round2 python -m bf_tap_r2.next_phase_probe --mode catboost --root . --seeds 42 3407
```

线性探针约 4 秒；CatBoost 增量检验约 5 分钟（40 次拟合）。

私有产物路径：`local/runs/round2-next-phase/linear-probe-r1/linear-probe.json`、`local/runs/round2-next-phase/catboost-increment-r1/catboost-increment.json`。
