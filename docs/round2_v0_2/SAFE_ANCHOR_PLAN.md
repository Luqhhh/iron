# 中位数基准与有界弱特征验证

从589bdeeb新建`round2-v0.2-signal-audit-and-safe-anchor`，先交付R2_M0_GLOBAL_MEDIAN，再完成新增配对复核与两个已知信号测试、铁口内联合置换、旧模型诊断、固定S25和唯一新路线Q2。此前v0.1和零拟合审计的结论作为历史保留，本轮不改写。

评分依据直接核对`初赛数据集/高炉铁次预测-1.pdf`第4/5页，ZIP依据第8页；SHA和适用范围见`configs/round2_v0_2/metric_contract.yaml`。本地参考分为max(0,100*(1-J))，不是平台回传或平台得分预测。PDF中的旧时间表入口已经被复赛快照README替代。官方2026复赛通知的历史最高生效、算分延迟覆盖旧PDF冲突表述；不预留“恢复高分”名额。新通知附件进一步修订仍未核对。

M0从2754行原始复赛标签计算未舍入全局中位数，model.json保存数值和来源，推理只读模型和测试索引。封包322行、准确唯一ID、规定列顺序、有限非负预测；ZIP只含result.csv。独立进程禁止读取训练数据，重建CSV须逐字节一致，并验证乱序与分批不变性。

本轮回归预算最多24次：2个合成已知信号Ridge测试、20次Q2、晋级目标最多2次全量基础模型训练。S25、置换、中位数统计、旧模型回读分别登记，不算回归拟合。不重跑旧70次、不升级依赖。Q2采用252项二阶展开、前后折内标准化、铁口独热、alpha=0.01的中位数回归及训练折目标中位数缩放。S25固定0.75训练折中位数＋0.25旧OOF，铁量L2、时长L1，不扫描系数。两路线均用旧的两组固定五折。

每目标晋级要求：两组整体OOF都优于对应M0；10折至少7折改善；所有划分/铁口退化≤0.001。均通过时按两组平均WMAPE选；极小差异优先简单者，数值容差在运行配置提前固定。全量审计不用于挑选Q2特征。没有晋级者也完成M0交付。

发布风险：已直接核对原PDF第7/9页的赛事数据禁止传播和公开展示条款。仓库用户授权不能替代主办方授权。本轮仅提交代码、配置、聚合摘要；额外核验相对589bdeeb没有原始数据变化，不改可见性、不重写历史。模型、预测、逐行误差、台账、ZIP、回执均留local/。

本轮仅本地准备，不自动上传或联系组委会。首日优先一次M0；挑战者只用通过门槛的单目标替换，组合需两目标均有平台正收益。机动名额用于实际交付修复或有依据的新候选，处理中不重复提交。
