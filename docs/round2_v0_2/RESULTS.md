# 复赛 v0.2 信号审计结果

结论：未发现本地连接错误，当前21特征和7个固定数值构造没有提供值得继续交互训练的相关性证据。结合v0.1两组OOF均输给中位数，本轮不启动额外模型扫描，不晋级提交候选。这个结果不能证明数据生成器错误、特征完全无信息或不存在更高阶非线性关系。

G0：Python 3.12.12 / uv.lock 全套588项测试通过，包括可检出人造强信号、置换确定性、常数列和非有限值处理及构造不依赖标签/ID。独立使用标准库CSV按sample_id字典查表，核对2,754×24=66,096个数值单元，特征、铁口与标签全部一致。P0原始文件摘要与上一轮汇总身份也通过核验。

G1：下表给出每个范围内全部原始特征×双目标×两种相关系数的最大绝对相关。每范围999次联合标签行置换，保留两个目标的相关性；置换p值比较的是整族最大统计量，不是从若干未校正p值里挑最小者。

| 范围 | 最大绝对相关 | 对应关系 | 置换最大值P95 | 最大统计量p |
| --- | ---: | --- | ---: | ---: |
| 全体 | 0.031709 | 焦比—时长，Pearson负相关 | 0.058628 | 0.878 |
| 铁口1 | 0.039545 | 炉喉温度—时长，Spearman负相关 | 0.084845 | 0.960 |
| 铁口2 | 0.058443 | 焦比—时长，Pearson负相关 | 0.083016 | 0.487 |

再对三个范围作Bonferroni校正，以上最小校正p均为1。该值表示此检验未拒绝零假设，不能解释成“无信号概率为100%”。校准依赖范围内可交换性；此前近重复审计未发现分组不等于证明独立同分布。

预先固定的7个数值构造单独作为探索检验族：

| 范围 | 最大绝对相关 | 置换最大统计量p |
| --- | ---: | ---: |
| 全体 | 0.019751 | 0.950 |
| 铁口1 | 0.036780 | 0.663 |
| 铁口2 | 0.038341 | 0.712 |

没有据此筛选或训练交互模型。铁口1中若干“铁口2指示量×特征”为常数0，按相关系数0处理，未当成异常记录删除。原始特征族与构造族是两个独立登记的探索范围，未声称它们共享同一个全局多重校正。

两目标自身Pearson相关为0.912808、Spearman相关为0.909413；spout_no与铁量/时长的Pearson分别为0.007991/0.001220。目标间强相关说明标签之间有关系，不说明当前快照特征能预测其中任一个目标。测试集没有真实目标，不能用真实铁量辅助测试时长预测，也没有因此启动跨目标预测链。

上一轮全部已保存OOF的预测—真值Pearson范围为−0.029549至0.015476。较复杂模型产生更大的预测波动，但未获得正向OOF收益。例如主验证铁量L2预测标准差7.988吨，C1为32.554吨，真实铁量标准差101.839吨；这只能描述当前模型，不是无信号的证明。

误差拆分显示问题不只来自边界：铁量边界28行、时长边界39行，所有路线在其余样本上的绝对误差和仍高于全局中位数基线。主验证铁量L2边界误差和反而下降21.657吨，但内部样本增加1046.597吨；时长L1边界下降2.924分钟，内部增加137.898分钟。因此没有依据把61.53写成预测下限或删除相关训练样本。

审计实现代码`d3d725b`，配置`configs/round2_v0_2/signal_audit.yaml`，本地证据`local/runs/round2-v0.2/signal-audit-r1/`。完整逐特征/目标/方法/铁口结果、OOF拆分、配置副本、原始SHA及审计源码身份均保存在Git外。`signal_audit.json` SHA-256：`547472b799ce62a194db82a0e11494fe47e9c1afa9dde891e45fead21c69226b`。

新增模型拟合0、预处理拟合0、平台上传0；v0.1的70次拟合及剩余4次全量训练预算保持原记录。全量训练数据上的信号审计是探索性分析，不能替代新的折内选择或独立测试。

当前最有价值的下一步是向官方核实新包的特征—标签配对及合成关联是否符合发布预期，不能将“模型没学到”直接认定为数据错误。未发送任何外部消息。可使用以下核验文字：

> 我们只使用复赛包，以sample_id进行一对一连接，独立核对所有特征和标签数值一致。固定两组五折下，线性、样条及CatBoost路线均未超过训练折中位数；公开21特征与双目标的整体线性/秩相关也很弱。请确认当前train_samples.csv与train_features.csv按sample_id的配对是否符合发布预期，以及合成过程中是否保留了特征与目标的预测关联。如果已有修订包，请提供版本或文件摘要。我们不要求测试标签或生成器细节。

在没有新增数据说明或正向验证证据前，不扩大参数扫描、不尝试恢复初赛编号/时间、不用平台额度追逐随机改进。
