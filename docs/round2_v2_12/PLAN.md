# V2.12 固定四项输入特征

上一轮残差分组只有弱、非单调线索，不是提分证据。本轮固定加入上一轮预先定义的四项输入：oxygen/(60*air_volume)、total_press_diff/air_volume、hot_air_temp-furnace_throat_temp、upper_press_diff/total_press_diff。保留全部21原数值列和spout_no。原模型白名单和包装器不改，单独FE4包装器显式记录25数值+铁口类别的列顺序。

所有新增列都是逐行确定转换，没有从全训练或测试数据拟合参数，没有使用目标或分位组均值。复用V2.11已测试的四项公式；训练、验证与最终推理使用同一构造。分母必须正，不隐式截断或插补。目标仍只按训练折均值归一化并还原原单位。

CatBoost配置固定为N1方案：RMSE、SymmetricTree、深度6、1500轮、学习率.03、l2=10、Plain/MVS、seed42、CPU4线程。FE4与已有N1比较输入表示效果，和C2铁量/完整B3时长比较晋级，额外与DJ/AORD比较。固定AFE4为当前参照/FE4原单位等权，不增加其余组合、公式、目标权重或种子。

两目标两组五折20次真实拟合；沿用原折分配、candidate-tiers-v1和2000次推荐候选重采样。评估阶段全量0，不自动封包；只有每目标入围者两组都优于现有最佳才推进另行发布。本轮若失败，不追加特征子集和比例扫描。原五包及顺序不变，用户自行上传。

冻结数据/折/参考OOF、四项公式源码与旧包摘要，独立进程回读全部模型；验证目标尺度、参数、列顺序、公式、树数、反序/分批/单样本仅输入推理、OOF及分类。工程合成测试拟合单独计数，运行完整锁定Python3.12测试。结果仍属多轮开发验证，不是平台成绩或独立确认。
