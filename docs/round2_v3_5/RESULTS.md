# Round2 V3.5 正则化 EBM、显式交互与组合约束搜索：实现与固定批次结果

日期：2026-09-24  
分支：`round2-v3.5-regularized-ebm-and-composition`  
基座：`fb689146d69147a14b56fe968d6a88a69a8bb5e3`  
状态：**固定 256 槽位与完整开发精筛已执行；最终 outer 23003 未消费；无新平台包、无上传。**

## 0. 一句话结论

V3.5 的 256 槽位固定批次、每目标 12 项完整开发精筛和 S0 cap 检查已完成。
- 完整开发组合候选（开发 seeds 42/3407 上选权，未做最终 outer）的包分为
  **96.2017**；同开发覆盖的 L1 包分为 **96.1510**。
- 该开发组合分 **低于已登记工作门槛 96.25**，因此本批没有进入最终 outer，
  没有生成 A-procedure / 新候选的最终同协议 `delta_A`，也没有条件追加。
- S0 cap 放宽只带来可忽略的单目标 WMAPE 变化；**保留原 `cap=0.50`**。
- 本轮 agent 上传数 0，平台包数 0；平台 >96.3 未被完成，也未用本地分推算平台分。

## 1. 参照与冻结口径

| 对象 | 分数 | 协议 | 本批用法 |
| --- | ---: | --- | --- |
| `V34_A` | 96.2684 | 用户回传平台分，未独立账号核验 | 当前已知平台最佳，仅作目标参照 |
| `V34_B` | 96.2660 | 用户回传平台分，未独立账号核验 | 历史第二 |
| A-procedure | 未运行 | 本批最终 outer 未消费 | 未给出 `delta_A` |
| 历史 A outer 16061 | 96.1997369853 | 历史 V3.4 outer | 只作历史辅助，不与新 seed 直接作差 |
| 同 outer L1 | 96.1603121558 | 历史 V3.4 outer | 只作历史辅助 |
| 开发组合候选 | **96.2017** | V3.5 开发 OOF，权重在 seeds 42/3407 共同学习 | 开发材料，不是最终 outer |
| 同开发覆盖 L1 | **96.1510** | V3.4 同协议 L1 OOF | 开发对照 |

说明：96.2017 与历史 A outer 96.1997 数值接近，但协议与样本覆盖不同，
**不得写成 `delta_A=+0.0017`**，也不是平台分预测。

## 2. 固定 256 槽位与 S0

### 2.1 预算枚举

| 路线 | 铁量 | 时长 | 小计 | 说明 |
| --- | ---: | ---: | ---: | --- |
| R 正则化 EBM | 72 | 72 | 144 | 3×3×4×2 网格 |
| I 特征表达/显式交互 | 32 | 32 | 64 | 2 中心×2 特征包×2 叶样本乘数×4 交互策略 |
| L 全局/局部 EBM | 24 | 24 | 48 | 2 中心×2 叶门槛×2 交互数×3 beta |
| 合计 | 128 | 128 | **256** | 无 N/A、无 duplicate、无 failed |

S0 另有 2 目标×2 专家集合×3 cap = **12 条新规则**，旧 `cap=0.50`
同协议控制另存，不冒充新基础模型。

### 2.2 实际 fit 记账

由于本轮 L 线执行时未启用跨 beta 共享缓存，以下 fit 数按实际执行记账：

| 批次 | 调度槽位 | outer fold 模型评估 | 基础 EBM/CatBoost 等 fit 单位 | 局部模型 fit | 按 outer_bags=4 展开的内部 bag 训练单元 | `fit_meta` 累计拟合秒数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| coarse-r1 | 256 | 512 | 704 | 192 | 2816 | 7803.5 |
| refine-r1 | 24 项×2 seeds | 240 | 360 | 120 | 1440 | 3079.5 |

- L 线 48 个评估槽位对应 **16 个唯一全局/局部组件**；本轮未共享 beta
  组件缓存，因此实际 fit 数高于唯一组件数。该事实不隐藏。
- `inner_bags=0`；`outer_bags=4` 是 EBM recipe 配置，不是额外候选选择。
- 失败数：coarse 0，refine 0。一次外层 shell 超时中断了 3 个待跑任务；
  它们按同一 trial identity 恢复补齐，未产生 failed、未重跑已完成项。

## 3. 固定批次开发结果

### 3.1 粗筛最佳单模型（seed42，folds 0/1）

| 目标 | trial | 路线 | pooled WMAPE |
| --- | --- | --- | ---: |
| 铁量 | `v35-s1-R-0047` | R 正则化 EBM | 0.037054327 |
| 时长 | `v35-s1-I-0181` | I 显式交互/特征表达 EBM | 0.039600074 |

### 3.2 完整五折、两 seed 精筛最佳

| 目标 | trial | 路线 | seed42 | seed3407 | 两 seed 均值 |
| --- | --- | --- | ---: | ---: | ---: |
| 铁量 | `v35-s1-R-0047` | R | 0.037884 | 0.038051 | 0.037968 |
| 时长 | `v35-s1-I-0181` | I | 0.039835 | 0.040063 | 0.039949 |

精筛选点使用了预登记的 6 个单模型强者、4 个对 L1 有正简单组合贡献者、
2 个结构不同者；每目标 12 项，seeds 42/3407 各完成 5 折。

### 3.3 开发组合

开发组合仅用于判断是否值得进入最终 outer，不是最终 outer：

- 铁量：`L1 + v35-s1-R-0036 + v35-s1-R-0041`，权重 `0.50/0.2518/0.2482`，
  两 seed 平均 WMAPE `0.0376158`。
- 时长：`L1 + v35-s1-I-0181`，权重 `0.6830/0.3170`，
  两 seed 平均 WMAPE `0.0383495`。
- 开发包分：`100 - 100 × mean(0.0376158, 0.0383495) = 96.2017`。
- 同开发覆盖 L1 目标 WMAPE 为 `0.0382734/0.0387058`，L1 包分 `96.1510`。
- 交叉 seed 评估：铁量 train42→eval3407 `0.0376052`、train3407→eval42
  `0.0376268`；时长分别为 `0.0382468`、`0.0384759`，均优于对应 seed 的 L1。
- 开发组合未达到 96.25，未冻结为最终 outer 流，也未计算 A-procedure 同协议
  `delta_A`。

## 4. S0 cap 检查

S0 使用已登记的 V3.4 A 专家身份，不重训基础模型：
- 铁量 G1：`v34-s1-ebm_boundary-0016`、`v34-s1-ebm_boundary-0020`；
  G2 由完整开发单模型表现选为 `v34-s1-ebm_boundary-0020`。
- 时长 G1：`v34-s1-ebm_boundary-0089`、`v34-s1-global_spout_shrink-0136`；
  G2 选为 `v34-s1-global_spout_shrink-0136`。

联合开发选择结果：

| 目标 | 最佳新规则 | 目标 WMAPE | 旧 cap=0.50 对照 | 判断 |
| --- | --- | ---: | ---: | --- |
| 铁量 | G1, cap=0.80 | 0.0376669 | G1, cap=0.50: 0.0376832 | WMAPE 改善约 `1.6e-5`，无实质增益 |
| 时长 | G1, cap=0.65 | 0.0384236 | G1, cap=0.50: 0.0384236 | 数值等同，无增益 |

结论：**cap 放宽没有形成可利用的完整 OOF 收益，保留原 `cap=0.50`**。
没有为新专家组合叠加 S0 放宽，也没有把 G1/G2/cap 的不同选择逐个当成最终 outer 消耗。

## 5. 哪些新成员实际被开发组合使用

- 铁量：`v35-s1-R-0036`（R 正则化 EBM，`max_bins=128`、
  `min_samples_leaf=100`、`interactions=10`、`max_interaction_bins=16`）与
  `v35-s1-R-0041`（R，`128/160/2/32`）。
- 时长：`v35-s1-I-0181`（I 表达 EBM，父中心
  `v34-s1-ebm_boundary-0089`、raw、叶样本乘数 2、I1 自动交互）。
- S0 最终未使用任何放宽规则。
- L 线单独有少量接近者，但未进入开发组合。

## 6. 未运行 / Blocked / N/A

| 项 | 状态 | 原因 |
| --- | --- | --- |
| 条件追加 64 | 未触发 | 未建立 A-procedure 同协议增益；开发分未达 96.25 |
| 最终 outer seed 23003 | 未消费 | 开发材料未达工作门槛，避免无候选时消耗新 outer seed |
| A-procedure 重放 | 未运行 | 没有冻结最终流；不以历史 A outer 冒充同协议 replay |
| 模型级冷审计 | 未运行 | 没有最终候选；搜索阶段不做逐 trial 发布审计 |
| 平台包/上传 | 0 / 0 | 用户未授权，且无候选达到准备条件 |

## 7. 限制与下一步

- 本批证明 R/I/L 固定网格在开发上有正的 L1-相对收益，但时长目标本地
  WMAPE 仍不足以把开发包分推到 96.25。
- 开发组合权重是在 seeds 42/3407 全开发预测上学习的；虽然交叉 seed
  评估为正，但它仍不是最终 outer，不能称样本隔离或平台增益保证。
- L 线 fit 共享未在本次执行中实现；下次若要严格按“48 评估槽位不等于
  48 套独立重训”记账，应先实现组件级共享缓存再重跑。
- 下一步若继续，应优先补强时长侧正则化/局部 EBM 的有效中心，或在条件追加
  预注册方向中做有触顶证据的训练长度扩展；任何最终流仍须先达到开发完整
  门槛，再消费 seed 23003。
