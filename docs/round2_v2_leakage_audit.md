# V2 泄露与异常审计

用户于 2026-09-22 要求检查训练/测试泄露及异常。仅访问 V2 公开训练标签和测试输入，不读取初赛标签，不读取或推断官方测试真值。

固定检查：文件与模型 SHA 身份、独立 ID 对齐、训练/测试 ID 及匿名后缀交叉、重复特征向量、训练 IQR 标准化 Chebyshev 最近邻（阈值 0/0.01/0.05）、空值与常数、模板标签、单特征 Pearson/Spearman 和精确线性变换、21 特征 KS 分布比较（Bonferroni 0.05/21）、行序/ID 关联、已保存 C2 实际特征名单和训练/验证误差。

字典将 pig 描述为“铁量”，但没有采集时间或因果可用性信息，因此预先固定两项诊断：种子 42 原五折，每折训练标签在铁口内成对打乱（两个目标同时置换），以及训练/验证同时把 pig 设为常数 0。保持 C2 所有参数和其余特征不变，共 20 次诊断拟合。模型与 OOF 保存本地，序列化回读及乱序推理核验。单次置换不是正式显著性检验，不据此挑选新模型或替换提交包。

```bash
UV_CACHE_DIR=/tmp/iron-uv-cache uv run --locked --python 3.12 --extra dev --extra round2 pytest tests/test_round2_v2_leakage_audit.py tests/test_round2_v2_release.py tests/test_round2_v2_compare.py -q
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 UV_CACHE_DIR=/tmp/iron-uv-cache uv run --locked --python 3.12 --extra round2 python -u -m bf_tap_r2.v2_leakage_audit --output local/runs/round2-v2/leakage-audit-r1
```

检查只能识别可见输入与本地训练流程中的问题。没有官方生成器、原始生产关联键、预测时点、测试真值及后台切分记录，无法证明因果可用性、生成器独立性或排除全部非线性标签代理。

## 审计结论

在本次检查覆盖范围内，**未发现直接训练/测试泄露证据**；未发现明显格式、对齐或单变量分布异常。不能据此证明全部数据生成与生产时点均无泄露。字典中 pig（铁量）的生产时点语义仍未核实，官方生成器和后台切分信息未提供。

G0：锁定 Python 3.12 下 18 项相关测试通过；20 次固定 C2 对照拟合完成，独立进程重新读取全部 20 个模型并校验 SHA、验证集真实标签、OOF 和指标，无新增拟合。既有 10 个种子 42 C2 模型核验输入名单与原摘要，实际输入仅 21 个数值特征加 spout_no，不含 ID 或目标。源码和输入与已完成比较的冻结清单一致。

### 数据与文件

- 训练 2754 行、测试 322 行；ID 及匿名后缀交集均为 0，独立 ID 字典重建特征完全一致。
- 训练内部、测试内部和跨集均无相同的 21 维特征向量。在训练 IQR 标准化 Chebyshev 阈值 0.01、0.05 下也未发现近重复。跨集最小距离为 0.49538；该检查不排除更复杂的生成依赖或隐藏共同来源。
- 无空值、无非有限数值、无常数数值列。测试无目标列，模板的两个预测列全部为空。字典无隐藏工作表、公式或外部链接，内容是字段/提交说明。
- 两个目标各有 2754 个不同值。42 个单特征—目标组合中，未发现同一行直接相等或最大残差不超过 1e-6 的仿射复制。不能排除任意非线性或多特征标签代理。
- 21 个特征均未在 Bonferroni 0.05/21 门槛下显示显著 KS 分布偏移；最大 KS=0.07096，最小未校正 p=0.10405，最大绝对标准化均值差=0.09311。测试值均处于相应训练最小/最大值范围内。单变量检验不能证明完整联合分布相同。
- 标签与 ID 数值/原始行序的绝对 Spearman 均小于 0.036；ID 与行序本来也未进入模型。

### 特征与标签

铁量与 oxygen 的 Pearson 为 0.73245，与 pig 为 0.66234；时长与 total_press_diff 为 0.68902。未发现单个字段与标签完全同值或精确线性变换。字典仅将这些字段描述为合成样本级指标，无法据此确定真实生产中何时能够观测。

C2 的铁量预测主要使用 air_volume、oxygen、pig、total_press_diff、consumption；时长主要使用 total_press_diff、furnace_throat_temp、hot_air_temp 等。重要性是已拟合模型的描述性度量，不等于因果贡献。

### 固定参数对照（G1）

使用种子 42 的原五折；数字是 OOF WMAPE/J 百分比，越低越好。没有使用平台分数挑选对照。

| 对照 | 铁量 WMAPE | 时长 WMAPE | J |
| --- | ---: | ---: | ---: |
| 原 C2 | 4.006564 | 4.101036 | 4.053800 |
| 训练标签在铁口内成对打乱 | 18.747942 | 17.549865 | 18.148903 |
| pig 同时遮蔽为常数 0 | 5.416375 | 4.084325 | 4.750350 |

打乱标签后精度退回基线附近，没有出现破坏训练 X/y 配对后仍保持高精度的异常。这支持模型依赖实际特征—目标配对，但不是对所有泄露机制的排除证明。遮蔽 pig 后铁量误差有所上升、时长基本不变，整体仍明显优于 M0（同折 J=17.689520%），因此不能将高分全部归因于 pig。

既有 C2 五折平均训练/验证 WMAPE：铁量 1.8056%/4.0056%，时长 1.8836%/4.1008%。存在正常的泛化差距，没有训练和验证均近零误差的现象；差距本身也不能证明不存在泄露。

综合判断：高分更符合有较强可学习信号、边际分布接近的合成任务表现。仍需官方生成逻辑、切分依据及特征采集时点，才能审计共同来源依赖、事后指标或生成方向。真实生产泛化能力不能由当前合成测试成绩推出。

运行目录：`local/runs/round2-v2/leakage-audit-r1`，实现提交 `ad26158`。明细、模型、OOF、台账和独立复核留在 Git 外。当前 C2 提交包与 96.0537 用户回传保持不变；本次没有新增提交包、平台上传或模型晋级。
