# V2.9 加权核固定检查

V2.8权重改善了同参数等权核模型，但gamma=0.005下训练误差也高。只完成一个固定检查：KW2使用同训练折C2的重要性权重，alpha=0.1、gamma=0.02；权重公式、所有输入、目标训练折归一化和类别编码保持V2.8不变。实现复用GeometryRegressor的KWT引擎，阶段候选身份单独记录为KW2。

比较两个已保存对照：V2.7 KR1（同alpha/gamma、等权）和V2.8 KWT（同权重、gamma=0.005）。旧模型不重训。固定AKW2为当前参照与KW2原单位等权，不追加比例、alpha或gamma。本轮两目标两组五折20次拟合，验证后不自动封包，全量预算0。

分类使用candidate-tiers-v1，当前参照C2铁量和完整B3时长；同时报告相对既有最佳DJ/AORD和两个核对照的差值。每目标最多一个formal、全部目标最多一个exploration。推荐候选2000次配对重采样，正式发布需要另行核对是否两组均胜过DJ/AORD。仍是重复使用的开发验证，不声称独立确认。

冻结旧数据、折分配、模型、OOF和待测ZIP摘要，20个新模型独立回读验证身份、折内尺度、输入顺序、权重、参数、预测及指标。维持五个待测包队列，用户自行上传；平台最高96.0982仍只是已登记用户回传。本轮若无改善，将保留负面证据，不继续围绕此核参数扫描。
