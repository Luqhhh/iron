# F-C / F-D 前置审计（2026-09-08）

本次继续优化时读取了本地原始赛题 PDF、发布包 README、字典和样本元数据。
未读取 November 目标；未复制或上传原始赛事资产及字典内容。

F-C：赛题明确将官方样本索引表列为输入，reference_time 为开铁时刻，要求只
使用当前参考时刻及之前的数据。因此允许基于官方已发生索引构造节奏特征，
实现采用更严格的 reference_time < current_reference_time。开发入口必须显式
传入官方 train_samples 路径，但解析器只取 sample_id/spout_no/reference_time。
部署时，各阶段所需的训练及当前/此前测试索引文件必须显式声明并固定身份；
不自动扫描目录补充索引。具体角色列表登记于 features_remaining.yaml。

实际文件元数据共 3,959 行（训练 2,754、test_a 335、test_b 322、test_c 548），
sample_id 无重复，三个字段无空值。仅声称相对声明文件的完整性，不声称完整
生产事件覆盖。实际文件没有同时间批次，但合成测试覆盖了同时间全部排除。
字典 Summary 的旧阶段划分与 PDF/实际文件不一致；仍以 PDF 和实际文件时间
身份为准，不改变 OPT-07 的跨度映射。

F-D：字典确认输入是事件级记录和五个数值指标。允许同字段相邻可见事件的
值差，以及可见事件计数；不足两个事件或相邻字段缺失时保留缺失。沿用 baseline
已有的事件年龄列，新增 5 个差值及 24/72h 两个计数，不复制年龄列。
不假设记录值在事件后持续生效，不做时长加权、不补造缺失的单位或物理关系。
源表 4,021 行，存在一个重复时间；继续执行 baseline 的完全相同记录去重、
冲突记录拒绝规则。

本次以 remaining-features-v1 单独解除前置阻塞，不回写旧 features.yaml 的
历史登记。仅运行 F-C/F-D 各自叠加到 E09 的独立消融，仍使用冻结 800 轮，
不新增参数或目标组合。计入本轮四项特征上限，与已跑 F-A/F-B 合计四项。
