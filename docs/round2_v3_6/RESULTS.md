# Round2 V3.6 损失函数、训练方式与数值编码网络：实现与固定批次结果

日期：2026-09-24  
分支：`round2-v3.6-loss-training-and-numeric-encoding`  
基座：`3db353f89c8efbc60aea0fcbd4b4220b93e25f62`  
状态：**固定 224/224 完成；O/D 与 N 铁量侧完整开发精筛完成；N 时长侧按有限预算停止规则停在固定批次；条件追加未触发；在用户显式指令下越过预登记门槛运行最终 outer 23003，但未通过；无平台包、无上传。**

## 0. 一句话结论

- 固定 224 槽位全部完成：O 64、D 96、N 64。
- 先补上了 **A 开发重放**：A_dev 完整包分 **96.19464675636728**，V3.5_dev 为 **96.20173099861380**，相对 A_dev **+0.00708424**。
- 在 A_dev 基座上，用 O/D/N 新候选中最多两个专家、新权重总和 ≤ 0.5 做开发组合，最好包分为 **96.20376256899247**，相对 A_dev **+0.00911581**；两个完整开发切分均为正。
- 未达到本地工作门槛 **96.25**；未达到条件追加门槛“平均完整包增益约 0.02”。
- 开发门槛未达到，条件追加未触发；用户随后显式指令运行最终 outer 23003。outer 仍未通过，因此**不生成平台 ZIP**。当前平台参照仍为用户回传的 V34_A = 96.2684。

## 1. 参照与 A 开发重放

本轮继续区分三个对象：

| 对象 | 分数 | 协议 | 用途 |
| --- | ---: | --- | --- |
| V34_A 原始提交包 | 96.2684 | 用户回传，未独立核验 | 平台最佳，保存原包 |
| A 开发重放 | **96.19464675636728** | seeds 42/3407 完整五折，A 固定配方 | 本轮候选的相对基准 |
| V3.5 开发组合 | **96.20173099861380** | 同 A 开发覆盖 | 历史发展材料，不是平台分 |
| A 铁量＋V3.5 时长 | **96.19834788917608** | 同覆盖 | 开发诊断 |
| V3.5 铁量＋A 时长 | **96.19802986580501** | 同覆盖 | 开发诊断 |

A 开发重放只复用已登记端点 OOF，不重训历史模型、不读最终 outer。结果说明：

\[
\Delta_{A,\mathrm{dev}}(\mathrm{V3.5})=+0.00708424,
\]

远小于 V3.5 相对 L1 的增益。后续新候选均报告相对 A_dev 的增量。

## 2. 固定 224 槽位

| 路线 | 铁量 | 时长 | 合计 |
| --- | ---: | ---: | ---: |
| O：EBM 损失、目标坐标与正则 | 32 | 32 | 64 |
| D：EBM 训练方式与平滑 | 32 | 64 | 96 |
| N：数值编码 MLP / TabM | 16 | 48 | 64 |
| 固定槽位 | 80 | 144 | **224** |

所有槽位均完成，无 N/A、无 failed、无重复 trial hash。最终固定批 ledger：

`local/runs/round2-v3.6-loss-training-and-numeric-encoding/fixed-r2-final/fit_ledger.jsonl`

### 2.1 O 线：损失函数与正则

父中心：
- 铁量：`v35-s1-R-0036`、`v35-s1-R-0041`
- 时长：`v34-s1-ebm_boundary-0089`、`v35-s1-I-0181`

扫描：
- 目标变换：训练折均值归一化、log1p
- 损失：RMSE、`pseudo_huber:delta=0.01/0.03/0.10`
- `reg_lambda`：0、10

O 线固定粗筛最强单模型：

| 目标 | trial | 父中心 | 设置 | pooled WMAPE |
| --- | --- | --- | --- | ---: |
| 铁量 | `v36-s1-O-0008` | `v35-s1-R-0036` | log1p + RMSE + reg_lambda=0 | 0.03714224 |
| 时长 | `v36-s1-O-0063` | `v35-s1-I-0181` | log1p + pseudo_huber δ=0.10 + reg_lambda=10 | 0.03952647 |

说明：
- `O-0008` 与父中心 `v35-s1-R-0036` 的固定配置在 pooled WMAPE 上一致，说明包装器重放一致。
- `pseudo_huber` 的 `delta=0.01/0.03/0.10` 已按预设搜索，未根据残差临时修改。
- 部分 `log1p + pseudo_huber + reg_lambda=0` 时长 trial 出现崩溃式预测，pooled WMAPE 约 1.0078；这是损失/尺度组合在该批次的失败证据，没有被隐藏或删改。
- 所有评价均还原到原始单位。

最重要的实现约束：请求参数和 `ExplainableBoostingRegressor.get_params()` 实际参数均被保存；任何被丢弃、不受支持或未生效的参数都会抛错，不会计为有效 trial。

### 2.2 D 线：训练方式与初期平滑

父中心：
- 铁量：`v35-s1-R-0036`、`v35-s1-R-0041`
- 时长：`v34-s1-ebm_boundary-0081`、`v34-s1-ebm_boundary-0089`、`v35-s1-I-0181`、`v35-s1-R-0109`

因子：
- 训练方式：原有贪心；纯循环 `greedy_ratio=0, cyclic_progress=True`
- 初期平滑：关闭；主效应 500 轮 + 交互 100 轮
- 每步树叶数：2、3
- 学习率：0.01、0.05

D 线固定粗筛最强单模型：

| 目标 | trial | 父中心 | 设置 | pooled WMAPE |
| --- | --- | --- | --- | ---: |
| 铁量 | `v36-s1-D-0029` | `v35-s1-R-0041` | 纯循环 + 平滑开启 + leaves=2 + lr=0.05 | 0.03705075 |
| 时长 | `v36-s1-D-0048` | `v34-s1-ebm_boundary-0089` | 原贪心 + 平滑关闭 + leaves=2 + lr=0.01 | 0.03958070 |

D 线说明：
- O/D 父配方分离，未把 O 线损失获胜项套进 D 线。
- `max_rounds` 保持 6000，逐阶段、逐 bag 停止轮数保存在 `fit_meta`。
- 纯循环同时设置 `greedy_ratio=0` 和 `cyclic_progress=True`，没有关闭两条更新途径。

### 2.3 N 线：数值编码 MLP / TabM

四种结构：
- `raw_mlp`
- `ple_mlp`
- `raw_tabm`
- `ple_tabm`

训练设定覆盖：
- MSE + Adam
- MAE + Adam
- Huber + Adam
- MSE + AdamW

固定粗筛最强单模型：

| 目标 | trial | 结构 | 容量 | 训练设定 | pooled WMAPE |
| --- | --- | --- | --- | --- | ---: |
| 铁量 | `v36-s1-N-0006` | PLE-MLP | small | Huber + Adam | 0.04005559 |
| 时长 | `v36-s1-N-0048` | raw-TabM | large | MSE + Adam | 0.04001774 |

N 线边界执行情况：
- 全部从随机初始化训练，只使用赛事训练数据。
- 标准化、PLE 分位点、类别词表和目标尺度只在当前训练部分拟合。
- TabM 训练时分别计算各子模型损失再平均；推理时平均子模型预测。
- 限制：N 时长侧固定单模型明显弱于 A_dev（0.03842）与 O/D 候选，按“有限预算、无强单模型则停止投入”规则，未继续 N 时长完整开发精筛。
- **PLE-MLP 修正**：初版 PLE-MLP 把在原始数值上拟合的 PLE bins 错用于标准化后输入；该路径已修正为“PLE bins 与输入使用同一原始数值表示”。旧 PLE-MLP 固定记录保留为失败/被替代证据，修正后的 16 项已在 `fixed-r2-final` 中重跑；raw-MLP、raw-TabM、PLE-TabM 路径不受该修正影响。

## 3. 完整开发精筛与组合

精筛对象：每条线每目标 top 4（N 时长侧按停止规则不进入）。完整开发 seeds 42/3407、五折。

A 开发重放为基准，组合限制为：

\[
\hat y = w_A\hat y_A + \sum_j w_j\hat y_j,\quad m\le 2,\quad w_j\ge0,\quad
w_A+\sum_jw_j=1,\quad \sum_jw_j\le0.5.
\]

最好开发组合：

| 目标 | 选中成员 | 权重 | 目标 WMAPE | 相对 A_dev |
| --- | --- | --- | ---: | ---: |
| 铁量 | A_dev + D-0029 + N-0005 | 0.60393 / 0.35979 / 0.03628 | 0.03754247 | WMAPE 改善 0.00014103 |
| 时长 | A_dev + O-0057 + D-0048 | 0.78110 / 0.09015 / 0.12875 | 0.03838228 | WMAPE 改善 0.00004129 |

完整包结果：

| 指标 | 值 |
| --- | ---: |
| A_dev 包分 | 96.19464675636728 |
| V3.6 开发组合包分 | **96.20376256899247** |
| `Δ_A_dev` | **+0.00911581** |
| seed 42 包分 / Δ | 96.20130932 / +0.01141496 |
| seed 3407 包分 / Δ | 96.20621582 / +0.00681667 |

结论：
- 两个完整开发切分相对 A_dev 均为正。
- 但平均完整包增益约 0.0091，**未达到约 0.02 的条件追加门槛**。
- 开发包分 **96.20376 < 96.25**，未达到本地工作门槛。


## 4. 最终 outer seed 23003（用户显式指令例外）

预登记规则要求开发完整包达到 96.25 后才消费最终 outer。由于用户显式指令“运行最终 outer seed 23003”，本轮把该次运行作为**记录在案的用户指令例外**执行；它不改变原门槛，也不表示候选已通过准入。

同一 outer 协议下同时重放 A 流程与 V3.6 流程：L1 下层成员、A 专家、V3.6 专家、内层权重与选择均限制在外层训练部分内部。

| 对象 | outer 23003 包分 | 铁量 WMAPE | 时长 WMAPE |
| --- | ---: | ---: | ---: |
| L1 | 96.13672893449238 | 0.03841736 | 0.03884806 |
| A 流程 | 96.18128719942528 | 0.03783645 | 0.03853781 |
| V3.6 流程 | **96.19003538073855** | 0.03773797 | 0.03846132 |

比较：

- `Δ(V3.6 − A_outer 23003)` = **+0.00874818**
- `Δ(V3.6 − L1_outer 23003)` = **+0.05330645**
- 逐折 `V3.6 − A`：+0.00453、−0.00308、+0.01756、+0.00816、+0.01656；**不是每折为正**。
- 最终 outer 未达到 96.25，`Δ_A` 也未达到 +0.02。

结论：用户指令要求的最终 outer 23003 已实际执行并消耗；V3.6 流程在 23003 上仍不满足最终准备条件，因此不生成平台包。

## 5. 门槛、条件追加与最终 outer 决策

| 阶段 | 条件 | 本轮结果 |
| --- | --- | --- |
| 追加最多 32 项 | 两开发切分相对 A 都改善，平均完整包增益约 0.02，存在明确有效中心 | 两切分为正，但平均增益 0.0091；不触发 |
| 进入最终 outer | 开发完整包 ≥ 96.25，两开发切分相对 A 都为正，冻结最多两套流程 | 开发包 96.20376；未通过原门槛 |
| 最终 outer seed 23003 | 仅在冻结流程后消费 | 用户显式指令例外执行；outer 包分 96.19004，未通过 |
| 新平台包 / 上传 | 仅在候选通过后 | 0 / 0 |

因此最终 outer 23003 已因用户指令被消费，但结果未通过；V34_A = 96.2684 仍为当前平台最佳，不生成微小变体提交，不推测账号额度。

## 6. 工程与验证状态

- 新增公开代码：
  - `configs/round2_v3_6/search.yaml`
  - `src/bf_tap_r2/v3_6_models.py`
  - `src/bf_tap_r2/v3_6_networks.py`
  - `src/bf_tap_r2/v3_6_reference.py`
  - `src/bf_tap_r2/v3_6_run.py`
  - `src/bf_tap_r2/v3_6_sampler.py`
  - `src/bf_tap_r2/v3_6_selection.py`
  - `src/bf_tap_r2/v3_6_report.py`
  - `tests/test_round2_v3_6.py`
- 私有产物保留在：
  - 固定批次：`local/runs/round2-v3.6-loss-training-and-numeric-encoding/fixed-r2-final`
  - 完整开发：`local/runs/round2-v3.6-loss-training-and-numeric-encoding/complete-dev-r2-final`
  - A 开发重放：`local/runs/round2-v3.6-loss-training-and-numeric-encoding/a-dev-replay.json`
  - 汇总：`local/runs/round2-v3.6-loss-training-and-numeric-encoding/v36-summary.json`
  - 最终 outer 23003：`local/runs/round2-v3.6-loss-training-and-numeric-encoding/final-outer-23003`
  - 最终 outer 私有驱动：`local/runs/round2-v3.6-loss-training-and-numeric-encoding/run_v36_final_outer.py`
- 测试：
  - `tests/test_round2_v3_6.py`、`tests/test_round2_v3_5.py`、`tests/test_optimization_v35_time_leaf.py`：21 passed。
  - 全量测试：**796 passed, 1 failed**。失败项为 `tests/test_round2_next_phase_v31_ref.py::test_default_path_still_requires_the_ledger`，它是环境依赖的历史测试：本机存在所查找的 ledger，因此不再按测试预期抛错；与 V3.6 代码路径无关。
- 已执行：最终 outer 23003（用户显式指令例外，未通过）；未执行：模型级冷审计、平台 ZIP、上传。

## 7. 限制与建议

- 本轮证明 O/D/N 新维度在开发上只有约 +0.009 的 A-relative 增益，不足以把 96.1946 推到 96.25。
- N 时长侧在固定单模型上没有强信号，不应因结构名称新而继续投入；N 铁量侧即使进入组合，权重也仅约 0.036。
- 若未来继续，应优先寻找能同时改善两个完整开发切分的更强中心，而不是在本轮已关闭的 O/D/N 近邻重复扫描。
- 最终 outer 23003 已按用户显式指令消费；结果仍低于 96.25 与 +0.02 的最终准备条件，不能重试同一 seed 寻求更好结果。
