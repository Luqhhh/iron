# T1 时长与 AJ 铁量：单目标候选交付

用户在 V2.3 离线实验完成后明确要求生成两份待测包。本次扩展仅新增一次全量 J1 联合训练，不新增 CV 训练、不改变先前离线配置或结果。T1 复用已有三个时长模型的 [0,1000) 树前缀；AJ 使用全量 J1 的铁量列与原 C2 铁量等权融合。两个新包均以 96.0982 的原 B 包为父包，未修改的列直接复制原 CSV 字符串。

现有 **V22_I_ONLY 原包不变，仍优先提交**。用户自行上传并回传，未执行平台上传；最新已知额度记录仍为 2026-09-22 剩余 0 次，不推算未登记活动。

建议提交优先级：**V22_I_ONLY → V23_AJ_I_ONLY → V22_T_ONLY**。第一项遵守既定首包安排；AJ 两组均超过 I2 的离线铁量水平、相对 C2 为 10/10 折改善，作为第二项；T1 增益最小，列第三。三个完整包分别与原 B 的 96.0982 比较，收到回传后另行更新最高分选择，不自动合并列。

## 三个待测包

| 包 | 铁量 | 时长 | 相对 B 改变行数（铁量/时长） | 桌面 submission 子目录 |
|---|---|---|---|---|
| V22_I_ONLY（原包） | C2/D4 等权 | 完整 B3 | 322 / 0 | round2-v2.2/V22_I_ONLY/ |
| V22_T_ONLY（新包） | 原 C2 | B3 三成员各前 1000 棵 | 0 / 322 | round2-v2.2/V22_T_ONLY/ |
| V23_AJ_I_ONLY（新包） | C2/J1 等权 | 完整 B3 | 322 / 0 | round2-v2.3/V23_AJ_I_ONLY/ |

每个子目录中提交文件均为 `Luqhhh_bf_tap_predict_round2.zip`，只含 `result.csv`，三列、322 个官方 V2 ID；使用 .17g 输出新预测。未生成任何同时替换两列的组合包。

ZIP SHA-256：

- V22_I_ONLY：`46c16936b13e18f3271f58f4de4aa18a87595f6a6612031710360c11f362b8fb`
- V22_T_ONLY：`ce2de4f0777f8f638e7bd9df7d9d91e951935ecd24552ec53de9483577e50cab`
- V23_AJ_I_ONLY：`c74b4af65f3dcbe256f3aa476f93ed8e323078ac3017acfc54efe4de9ab8021b`

## 分数参考：不是可靠的平台预测

本地参考分为 100 × (1 − J)，J 使用两组完整 OOF 的等权双目标 WMAPE。平台算术参考 = 用户回传的 B 包 96.0982 + 候选本地参考分 − B 包本地参考分（95.9799338946）。这是假设 OOF 增益完整等幅迁移的计算，不是经校准的期望值，也不是置信区间。

| 包 | 本地 OOF 参考分 | 相对 B 的本地分增益 | 假设性平台算术参考 |
|---|---:|---:|---:|
| V22_I_ONLY | 96.0038 | +0.0238 | 96.1220 |
| V22_T_ONLY | 95.9838 | +0.0038 | 96.1020 |
| V23_AJ_I_ONLY | 96.0116 | +0.0316 | 96.1298 |

实际平台分可能低于 96.0982。此前 A 在本地改善但平台下降；已有四组结果不足以建立稳定偏移或预测区间。AJ 与 V22_I_ONLY 的算术差距仅约 0.0078 分，不能据此确定隐藏测试排名。T1 的离线分增益约 0.0038 分，尤其容易被样本差异淹没。无论如何，原定 V22_I_ONLY 首包优先级保持不变。

## 工程与训练证据

G0：3 项新增定向测试通过，完整锁定 Python 3.12 测试集 **660 项通过**。禁止读取训练数据的独立冷推理进程生成 CSV，与两个正式 CSV 逐字节一致；完整、反序、分批和单样本预测数值完全一致。两份桌面 ZIP 摘要与本地一致，原 V22 和原 B 包摘要不变。

本次真实拟合：CV 0，全量 J1 1；V2.3 累计真实拟合为 CV 50、全量 1。新增定向测试没有拟合；本次完整 pytest 父进程单独记录 71 次小规模估计器拟合（63 CatBoost + 8 其他），不计入真实赛事拟合。G1 仍只有离线依据，三份待测包尚无用户平台回传。

私有运行：`local/runs/round2-v2.3/candidate-release-r1/`；模型、模型身份与实际参数、拟合账本、发布清单、冷推理 CSV、正式 ZIP、分数算术记录和 COMPLETE 均留在 local/。

生成入口：`uv run --locked --python 3.12 --extra round2 python -m bf_tap_r2.v2_candidate_release release --output local/runs/round2-v2.3/candidate-release-r1`。本次已执行完成，既有路径拒绝覆盖，不应重复运行。
