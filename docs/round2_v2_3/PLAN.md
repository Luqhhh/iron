# V2.3：归一化损失与联合学习

冻结当前铁量 C2、时长完整 B3 为分类参照；额外报告对 V22 铁量 I2、时长 T1 的差值。继续使用原 42/3407 两组五折，是重复使用的开发验证，不是独立确认。

本轮固定 N1（独立 RMSE）、H1（独立 Huber:delta=0.05）、J1（一个双输出 MultiRMSE），以及当前目标参照与 H1/J1 的原单位 50/50 融合 AH/AJ。每个训练折单独计算目标均值 s，训练 (y-s)/s，预测 s*(1+z)。不加特征、种子、样本权重、早停或超参数搜索；模型保存实际参数、特征/目标顺序、尺度及训练身份。

真实训练预算：N1 20 次、H1 20 次、J1 10 次，总计 50 次交叉验证拟合；本次离线工作不做全量拟合、不生成新提交包。测试的小规模合成拟合另计。

直接接入 candidate-tiers-v1：每目标最多一个正式推荐，跨目标最多一个探索推荐。固定平局顺序 N1/H1/J1/AH/AJ；先选单模型，结构相同则使用登记顺序。完整报告所有路线的两组指标、两层差值、逐折与逐铁口指标、风险原因。推荐路线做 2000 次铁口内 ID 配对重采样，不作淘汰门槛。H1 与 N1 的比较及按当前参照绝对残差五分位划分的贡献仅作诊断，不参与训练。

2026-09-22 用户最新反馈剩余 0 次；新记录追加在本阶段，不改旧报告剩余 1 次的历史快照。2026-09-23 首包仍是已交付 V22_I_ONLY，SHA-256 `46c16936b13e18f3271f58f4de4aa18a87595f6a6612031710360c11f362b8fb`。其铁量 C2/D4 等权、时长完整 B3，禁止覆盖、改列或重训。新候选无论离线更好与否，都不撤换该首包；AH/AJ 中的参照冻结，不随未来平台反馈静默变更。

执行入口：`python -m bf_tap_r2.v2_robust_joint evaluate --output local/runs/round2-v2.3/robust-joint-r1`。回读入口 `verify` 不训练，在独立进程恢复所有新折模型和原 V22 参照、比较指标与分类，核对源文件和已交付包摘要。所有模型、逐样本预测、指标及额度反馈记录保存在忽略的 local/。
