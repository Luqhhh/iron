# v0.36：最后一次初赛提交的固定时长等权融合

状态：已登记、待真实 payload 核验与封包。起点为 `70eebab3c6f469ff2b9f0dc9baf42d0076de0f06`，分支为 `optimization-v0.36-single-slot-time-blend`。

唯一候选 `V36T_V34_V30_EQUAL_BLEND` 逐字符串复制 V34T 铁量，并在两个完整六位端点之后对 V34T、V30A 时长执行整数微单位 1/2 平均，半微单位按 ties-to-even。不得移动门控、收缩或舍入位置，不扫描比例，不生成第二候选。

用户说明只剩一个初赛提交槽，且没有初赛晋级分数要求。该约束未由 agent 独立核验。V35A 登记为 `UNTESTED_SKIPPED_SLOT_REALLOCATED`，V35B 的 83.2778 保留原证据等级，V34T 83.3201 继续作为最高用户回传原包，但本轮没有恢复上传预算。

两份源身份固定为：V34T result/ZIP `80a74e…6466` / `e3f970…3924`，V30A result/ZIP `97b6c3…aed9a` / `fffcf2…986`。缺源时只允许按摘要恢复旧 payload，不重训理论等价模型。

执行顺序为：来源/查重 → 锁定实现 → 可选历史端点回放 → 唯一最终 payload → 独立回读与冷重建 → 冻结一个 ZIP。模型、树、预处理器、校准器和系数拟合全部为 0；平台上传、桌面写入和恢复上传均不由工具执行。

理想未舍入且评测身份一致时，绝对误差凸性给出条件界 `S_mix >= (83.3201+83.3175)/2 = 83.3188`。考虑端点四位显示舍入与新六位舍入时，只能写成 `S_emitted >= 83.31875 - 50*n*(0.5e-6)/D_T`；隐藏时长分母未知，因此这不是平台保证。
