# optimization-v0.27 实施规格

基点为 `193842e81385f51efd48792df652fc03e30cc812`。本轮同时冻结两个候选：A 以 absolute-error QRF 完整替换铁量分支；B 冻结 V26A 时长森林，仅在每棵树的查询叶内按 60 日半衰期独立归一化训练响应权重。两项均以完整 V26A 为父方案，分别保持另一目标的六位 CSV 字符串不变。

预算为 A 六次开发加一次最终森林拟合，B 零森林拟合、七份训练 ID 对齐的 binary64 权重附件；新预处理、CatBoost、校准拟合均为零。开发使用 06–11 cutoff，最终仍为 2024-12-01 01:44:00+08:00 与 2,754 行。原 v0.15 gate、V21 的 25% 收缩及查询日 60 日中位数保持不变。

两个模型、预测与 ZIP 必须在任何本轮平台反馈前同时冻结；平台次序 A→B、上限两次、禁止自动上传和第三个组合包。G0 工程有效性与 G1 已消费历史标签上的模型质量分开报告。
