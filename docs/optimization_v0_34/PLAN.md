# v0.34：OOB 叶中位数的等树平均

状态：已登记实施规格；真实赛事运行、冷审计与封包完成情况由同目录 `RESULTS.md` 后续记录。

基点为 `optimization-v0.33-qrf-feature-and-row-sampling@e8dda75a93f159620d9f70ae0cfeed3e2cbf68d0`，实施分支为 `optimization-v0.34-oob-leaf-median-bagging`。父方案为 V30A，用户回传 83.3175。v0.33 A=83.2241、B=83.2638，两个固定设置均关闭。

本轮同时冻结两个零拟合实验：A 只替换铁量 QRF 端点，B 只替换时长 QRF 端点。源森林分别为 v0.27 铁量森林和 v0.26 A 时长森林；成员集合逐树复用 v0.29 的认证 OOB 附件。每个 selected leaf 在原始响应上取较小中位数，随后对 256 个叶点做等权算术平均。偶数集合取较小中间值，不调用 `forest.predict()`，不按叶大小、方差、时间、铁口、bootstrap 重数或平台成绩加权。

A 从原 V26A 完整 CatBoost 铁量端点与新 QRF 端点重新组成原 50/50 `mean6`，时长逐字符串复制 V30A。B 对新时长端点执行原六位 round-trip、原 v0.15 gate 和 V21 移动 60 日中位数收缩，铁量逐字符串复制 V30A。切回原 v0.29 合并响应分布的较小中位数时，两条完整链都必须精确恢复 V30A。

预算为 0 次模型、树、预处理和校准拟合；生成 A/B 各 7 份、合计 14 份监督叶点表；只读复用 14 份 v0.29 附件；最多生成两个候选包。平台候选预算为 A→B 各一次，但自动上传、恢复上传和桌面写入均为 0。两份定义及包必须在任何本轮平台反馈前同时冻结，不生成第三组合。

开发覆盖 June–November，最终 cutoff 为 `2024-12-01 01:44:00+08:00`。离线报告 H1–H4、J、DEV，主口径为 origin 宏平均，V30A 为主参照，V1 与 V21_REPLAY 保留。A 必须满足时长完全不变，B 必须满足铁量完全不变；逐 cell 与宏平均均检查 `ΔE = 0.5 × ΔWMAPE_changed`。

冷审计覆盖两个目标的全部七个 cutoff，独立恢复源模型、响应、预处理器、bootstrap/OOB 附件，重算叶点表及预测，并检查全量、反序、分块、子集、单样本和 ZIP/CSV 回读。所有 fit 入口拦截计数必须为零。私有模型、响应、叶点表、预测、账本、回执和 ZIP 不进入 Git。

公共实现和证据更新在检查通过后按仓库 `AGENTS.md` 及时 commit 并普通推送当前分支；这不授权平台上传、桌面写入、删除文件或公开私有赛事产物。
