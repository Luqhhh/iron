# 仓库协作与平台测试策略

执行本仓库任务前，读取并遵守根目录 `AGENTS.md`。本文补充 Claude 的平台测试安排，不取代仓库的数据保护、验证、私有产物与 Git 发布规则。

## 平台测试优先级

- 当前已回传的 V2 Top 5 中，平台与本地 OOF 分数相差约 0.09～0.11 分，绝对差异总体不大，但足以改变分数接近、小幅提升候选的排序。这不是仅针对低分模型的现象，也不能推广为固定的平台分数修正值。
- 后续优先推荐相对当前目标参照有较大离线提升，或具有明确、重要探索价值的提交包。探索方向须说明要验证的问题、已有证据与验证成本，不以名称新颖作为推荐依据。
- 小幅提升、与已测方案高度相似的包降低测试优先级。保留其证据和原分类，不认定无效或永久排除；不为凑包数量或用完每日额度安排上传。
- 此原则指导人工发布与平台排程，优先于仅按 formal/exploration 标签安排上传的旧表述；不改变自动分类、冻结门槛、探索推荐上限或历史结果。不临时增加“大幅提升”的数值硬门槛；如需设置，在新阶段评估前冻结。
- 当前参照从 `EVIDENCE_STATUS.json` 的 `round2_current_platform_best` 或最新 `round2_current_candidate_queue` 读取。**2026-09-26 的最新快照为 `V5_TIME_N0048_Q20`，用户回传 96.3143（第二轮已关闭，无待测队列）。** 历史快照：`V36_USER_REQUESTED_OUTER_FAILED` 96.2734；其后 V34_A 96.2684、V34_B 96.2660，最新探索包 V42_IRON_N2_Q25 为 96.2533（−0.0201）。** 历史 AJ3 96.1259 仍保留，但早已不是当前基准。

## 上传与交付边界

- 始终由用户自行上传并回传分数，助手不执行平台上传。用户说明每日上限 5 次，不再重复询问上传模式或剩余额度，不根据文件数或零散回传推算实际剩余额度。
- 发布前仍须核验数据、模型和包身份，按同验证折计算 OOF，并完成独立回读与冷推理检查。分类通过不代表自动授权训练、封包或上传。
- 单目标测试明确父包及比较分数；未修改的列保留父包 CSV 字符串（V4.2 封包曾用 `pd.read_csv`＋`str()` 重排父包时长列，已修正为直接透传 CSV 字段文本；见 `src/bf_tap_r2/v4_2_package.py` 与 `tests/test_round2_v4_2_packaging_fix.py`）。不得将尚未获得平台反馈的目标组合默认为已验证收益。
- 桌面 `submission` 的历史包与验证证据保留在私有 `local/`。**当前平台首选是 `V5_TIME_N0048_Q20`（96.3143，用户回传）**，不是历史 AJ3 或 V36；第二轮已关闭、无待测队列，不要自行恢复旧包或新增待测包（见文末收尾节）。
- 平台成绩明确标注用户回传，未经独立账号核验不称为已核验；本地收益不作为平台收益保证。

详细分类与排程说明见 `docs/candidate_tiers.md`；本批平台回传见 `docs/round2_final_top5/FEEDBACK.md`。

## Round2 V3 本地搜索状态

- 该历史 V3 搜索轮的平台参照为 AJ3 铁量＋完整 B3 时长，用户回传 **96.1259**；当前 V3.4 参照见下方 2026-09-24 节。
- `round2-v3-local-search` 首轮 400/400 项粗筛已完成；`xgboost==2.1.4` 已作为 `round2` 可选依赖安装，XGBoost 40 项完成但没有进入最优融合。
- CatBoost＋现有 OOF 的嵌套融合本地完整包约 **96.100735**，相对 AJ3 本地参照 **+0.088347**。
- 已按 seeds 42/3407 冻结外层权重，并在未参与选择的 seed 2026 上复验：本地完整包 **96.116174**，相对同 seed AJ3 参照 **+0.088335**，收益保留，仍为 `candidate_pool`。
- 尚未封包、未冷审计、未上传；是否封包需用户明确授权。

## Round2 V3.4 下一阶段（2026-09-24，历史背景）

- 用户回传：`V34_A = 96.2684`，`V34_B = 96.2660`；来源为用户报告，未独立账号核验。
- 当前平台最佳为 `V34_A`，`V34_B` 第二；历史 AJ3 `96.1259` 保留为历史证据，不再作为当前基准。
- 下一阶段平台目标：**> 96.3**。
- 预登记本地工作门槛：**≥ 96.25**；本地冲刺目标：**≥ 96.30**。依据是已观测本地-平台差约 `+0.069 ~ +0.114`，但本地分不是平台分保证。
- **原则上未达到本地工作门槛、未通过同协议 outer 验证的候选，不消耗平台测试额度。** 本地分或本地 delta 不能替代验收。
- 实验要求 **大范围优化搜索**：继续宽范围本地探索，不做已测方案的小幅微调凑数。覆盖结构、EBM 边界/残差、全局/分铁口专家、受约束组合和目标隔离回退；继续遵守数据保护、去重、append-only 缓存身份、候选冻结与冷审计规则。
- 新候选至少两个完整开发切分为正，才允许消耗新的最终外层 seed；例外必须预先登记并说明理由。
- 用户自行上传并回传分数；助手不自动上传、不自动为平台封包、不消耗测试额度。
- 分数转移与门槛分析：`docs/round2_v3_4/SCORE_TRANSFER_AND_NEXT_TARGET.md`。

## Round2 V4.2–V4.5：机制已测，残差路线关闭（2026-09-25，历史背景）

当前平台最佳仍是 **`V36_USER_REQUESTED_OUTER_FAILED` = 96.2734**（用户回传，未独立账号核验）；下一阶段目标仍是 **> 96.3**。V4.2–V4.5 四轮**没有产生任何晋级候选、包或上传**。

两个事实约束下一轮：

1. **本地增益不转移。** `V42_IRON_N2_Q25`（V36 铁量列 + 0.25 × V4.2 `N-N2`）平台 **96.2533**，相对 V36 **−0.0201**，而它的本地同协议增益是 **+0.0144**。已核验配对的本地-平台差并不恒定（V34_A `+0.0687`、V36 `+0.0696`、V42_IRON_N2_Q25 `+0.0347`），**差值的波动大于被选择的增益本身**。
2. **本地的排序信号弱于效应。** N2 混合的逐折增益跨 `−0.009 ~ +0.028`；V34_A 最终外层五折跨 `+0.0078 ~ +0.0641`（均值 `+0.0394`）。预登记的粗筛门槛 `+0.005` 就落在噪声里。

**已由实测关闭——没有新证据不要重开：**

- **NODE 逐深度（N4）与 ODST 核心（N5）**：两个目标都未过粗筛（N4 `+0.00094 / −0.00093`；N5 `−0.00311 / +0.00459`）。修复后的共享选择器对照 N2 在 `tap_iron` 过粗筛（`+0.00901`），随后在完整十格上未过融合门槛（`+0.014934`，8/10 折）。见 `docs/round2_v4_4/RESULTS.md`。
- **TabR 完整融合（R4）与无检索对照（R5）**：六个单元全部未过粗筛。R4 − R5 = `+0.0176 / +0.0037`（检索通道确实有效），但 R4 − R2 = `+0.0123 / −0.0118`（融合收益不跨目标复现），整族远低于 `B_fit`。
- **残差修正器**：强基座折外残差**没有可预测的方向**（平均 sign AUC `0.5092` / `0.5070`；拟合的符号模型 Brier 比校准常数更差；`P(r>0)` 为 0.4938/0.4996，而残差标准差是 26.07/6.23）。三个预登记修正**全部**让样本外 MAE 变差。见 `docs/round2_v4_5/RESULTS.md`。
- **时序/滞后特征不可用，不是"没试过"**：`复赛_train/train_features.csv` 只有 `sample_id` + 21 个瞬时特征，没有任何时间戳或顺序列。

**剩余杠杆所在：** V36 权重高度集中（`tap_iron` 0.604 A + 0.360 D + 0.036 N；`tap_time_len` 0.781 A + 0.090 O + 0.129 D）；`tap_time_len` 每个绝对误差单位的价值约为铁量的 **4.2×**（WMAPE 分母小 4.2 倍）；当前实际收获的是**分散化增益**（N2 候选单独更差，MAE 21.4 对 20.2，但只 0.887 相关，0.25 混合赢 0.16 MAE）。`local/runs/round2-v3-local-search/*/pred-*.npy` 已有 **482 个文件 / 444 个 trial** 的 OOF 预测池，做显式的误差协方差选成员**不需要新的模型拟合**。注意 V3.4 的 "diversity" 是**结构去重**，不是误差相关性筛选。

**下一轮的流程约束：** 在相信任何 `+0.01` 量级候选之前，先把本地评估分辨率提上去（更多 split seed、配对标准误、用下置信界而不是均值做准入）；并把后续每一次提交当作需要登记的**实验**，而不是例行额度。

- `round2-v4.2-n2-seed-repair` 上的实现提交：`2208bd1`（V4.4 机制）、`d774195`（N 线经已提交代码逐位复现）、`91dded2`（V4.5 残差诊断）。
- 这几轮期间有第二个 agent 会话在同一 checkout 中工作；它撰写了 `src/bf_tap_r2/v4_2_followup.py` 与 V4.2-r2 修复。碰撞记录见 `docs/round2_v4_2/FOLLOWUP_RESULTS.md` 第 8 节。


## Round2 V5：误差协方差、分辨率与平台噪声底（2026-09-25，历史背景）

分支 `round2-v5-error-covariance-resolution`；预登记 `configs/round2_v5/SPEC.yaml` 与
`docs/round2_v5/PREREGISTRATION.md`（提交 `f0d7900`）；实现提交 `06f956c`；结果见
`docs/round2_v5/RESULTS.md`。**本轮已超过 96.3 目标：`V5_TIME_N0048_Q20` 平台 96.3143，相对 V36
父包 +0.0409。** 共交付 2 个由用户上传的包——已声明的噪声底对照包与晋级候选包；助手未执行任何上传。

**必须纠正的前提：** `local/runs/round2-v3-local-search/*/pred-*.npy` 的“482 文件 / 444 trial、
两个 split seed 都在”不成立。482 个文件只对应 **400 个不同 trial id**，其中只有 **38 个 trial**
在 seed 42/3407 上具备完整五折覆盖；其余 400 个是 **seed 42、仅 folds 0/1（1102/2754 行）** 的
coarse 批次。同一 `trial_id` 在 `coarse-*`（开 ES）与 `refine-*`（关 ES）中是**两次不同拟合**，
库键必须为 `(来源目录, seed, trial_id)`。真正的零拟合库：铁量 114 / 时长 110 条双 seed 完整列。

**实测结论：**

- **现有池已榨干。** 按预登记规则（`ρ ≤ 0.95`、单独 WMAPE `≤ 1.25×` 基座、α 内部），60 个被筛查
  候选中 16 个嵌套均值为正，**0 个**通过折级门槛；最佳 `v3-mlp-tap_time_len-0000` 为 `+0.0055`
  分、7/10 折为正；最佳“删成员再替换”为 `+0.0057` 分。留一显示 `N-0005`、`O-0057`、`D-0048`
  合计权重 0.255 却只贡献约 0.002 分。
- **时长列 N 家族的缺失是真问题，补上后接近门槛。** V3.6 以“单模型弱”停掉时长 N 线，而铁量 N
  单模型同样弱却贡献了全部增益。补全后 `v36-s1-N-0048`（large raw-TabM）取得 **`+0.00978` 分、
  8/10 折为正、两 seed 同向（+0.0121/+0.0075）、ρ=0.888、精度比 1.07**，折级 LCB 仅 `-0.00015`；
  轻量 N 候选只有 `+0.002~0.003`。该族价值在相关性，不在单模型分数。
- **本地分辨率才是瓶颈。** 只平移训练 seed（配方/权重/专家/切分不变）本地就移动 `-0.00496`
  （铁量）与 `+0.00035`（时长）；`shift=0` 重建与父包列相对差 `2.6e-16`。候选效应与纯 seed 改写
  同量级，故后续晋级必须走“≥4 split seed”规则，而不是均值门槛。
- **折级下置信界没有保护力。** 已记录的 N2 十格给出正的折级 LCB（`+0.0067`，8/10），而该候选在
  平台亏 `0.0201`；V5 规则拒绝它靠的是“≥4 split seed”。该算术已固化为回归测试。
- 回溯校准：4/4 已知坏候选被拒绝；旧的“均值 ≥0.005 同号”门槛会放行其中 2 个。

**下一步（平台）：** `V5_SEED_SWAP_S1000`（ZIP SHA-256
`4ff97c033f05b7e686453e913460b548cd7231d3736b3f431bb3882e1a82460f`）已就绪并通过零平移复现校验，
即冻结 V34_A 端点 + 四个冻结专家、仅训练 seed 平移 +1000；**由用户上传**。判读：对 96.2734 的
`|Δ| ≈ 0.02` → 停止千分位追逐；`|Δ| ≈ 0.005` → seed 敏感度占主导，误差协方差方向继续并强制
“≥4 split seed”准入。**Stage 2b 已通过。** `v36-s1-N-0048` 在派生 seed `7777`/`12011` 上完成复现（每折用
`V36FixedRecipeFactory` 重拟基座；基座列作为混合端点，权重在其余三个 seed 上选择）：逐 seed 增益
`+0.01233 / +0.00808 / +0.00970 / +0.00876`，均值 `+0.00972`、sd `0.00186`、**seed 级配对
LCB95 `+0.00753`、4/4 为正**；派生 seed 的基座（`0.03825/0.03842`）与已记录 seed
（`0.03829/0.03848`）同量级。这是本项目首个通过独立 split seed 门槛的候选。保留意见在折级：
20 格中 14 格为正（70%，参考比例 80%），按 SPEC 记为 `descriptive_only` 并随附在
`decision.fold_criteria`。**预期本地完整包 `≈96.2135 < 96.25`**，故仅进入 `candidate_pool`，
未生成平台候选包。

复现期发现并修复三处实现缺陷（均已加回归测试）：折级门槛原为绝对计数 8 而非 SPEC 的 8/10
比例（20 格时只需 40%）；`admit` 把标注为 `descriptive_only` 的折级当成硬门槛；复现脚本最初拿
**未混合的候选**与基座比较，产生 `−0.11~−0.14` 的假结果（在任何结论落盘前发现）。修复后重跑
Stage 1：回溯校准仍是 4/4 拒绝，完整十格 N2 仍只被 `insufficient_split_seeds` 拒绝。

**噪声底实验已回传（用户报告，2026-09-26）：** `V5_SEED_SWAP_S1000 = 96.2739`，相对 V36 **+0.0005**，
而该包的本地 seed 敏感度是 `−0.0046`。两个后果，且必须替换此前的读法：

- **平台对该类扰动的分辨率约 0.0005** —— 平台不会在 0.01–0.02 量级抖动，“本地增益不转移是平台噪声”
  这一解释被否证；
- 当初由该对照导出的“干净对照转移误差约 0.005”**已作废、不得复用**：紧随其后的候选实测转移误差为
  `+0.031`，说明**转移误差是候选相关的**，可在任一方向达到本地效应的数倍。本地证据只可用于
  **方向与排序**（限四 seed 规则筛出的候选），不可用于预报幅度。V42 倒挂（本地 `+0.0144`、
  平台 `−0.0201`）仍更应归因于**本地选择过拟合**（当时只有 2 个 split seed）而非平台噪声。

`V5_SEED_SWAP_S1000`（96.2739）作为噪声底对照保留。`v36-s1-N-0048`（四 seed 复现，LCB `+0.0075`）
本地预测平台效应 `+0.005 ~ +0.015`，**实测 `+0.0409`**——正是这一对比使“本地幅度可用于预报”的读法
被撤回，也使冻结的本地工作门槛 `96.25`（本会挡下该候选）被记为过度保守。是否据实测重订门槛属用户
决策，尚未调整。

**目标已达成（用户回传，2026-09-26）：** `V5_TIME_N0048_Q20` = **96.3143**，相对 V36 **+0.0409**，
**超过 96.3 目标 +0.0143**；已登记为 `EVIDENCE_STATUS.json` 的当前平台最佳。配方：冻结 V36 父包只替换
`tap_time_len` = `0.8×V36 + 0.2×v36-s1-N-0048`（large raw-TabM 全量重拟），`tap_iron` 逐字节不变
（0 处不匹配，回读混算 diff 0，冷推理 diff 0）。ZIP SHA-256
`5ed99b8014fb1650b88b6ae57cc3ed4dac378a20831eab5efc800de91ab2c982`。

三条被实测确认/推翻的结论：

- **四 seed 规则首次实盘通过**：该候选在两个从未参与选择的派生 seed 上为正，平台兑现为本地预测的
  **4.19 倍**（本地 `+0.00975`，平台 `+0.0409`；隐含测试集时长列相对降幅 2.13%，而折外只有 0.51%）；
- **本地幅度被压缩**：本地只能用于排序与定向，不能预报幅度；
- **冻结的本地工作门槛 96.25 被证明过度保守**：它本会挡下这个候选（预期本地完整包 96.2135）；本候选
  的本地-平台偏移 `+0.1008`，历史区间 `+0.0347 ~ +0.1008`。是否据实测重订门槛属用户决策，本轮未改阈值。

**时长列这条路在现有库内已到顶。** 四个 large N 成员彼此残差相关 0.979–0.998（本质同一模型），
叠加无增益；把相关最低但精度较差的 `JM1`（ρ=0.777）与 `N-0048` 组合，权重被优化为 0 且 held-seed
增益不增。要再往上需要**新的模型族（新拟合）**，而不是在同一池里继续搜成员。

**关闭路线不变**（NODE 逐深度、ODST 核心、TabR 完整融合、残差修正器、样本重加权、密集 α 扫描）；
时序/滞后特征属**不可用**（`复赛_train/train_features.csv` 无时间戳或顺序列），不是未尝试。

## Round2 收尾（2026-09-26，当前指令）：目标已达成，第二轮关闭

**最终平台最佳：`V5_TIME_N0048_Q20 = 96.3143`**（用户回传，未独立账号核验），相对前一最佳
`V36 = 96.2734` **+0.0409**，**超过 96.3 目标 +0.0143**。配方：冻结 V36 父包只替换
`tap_time_len` = `0.8×V36 + 0.2×v36-s1-N-0048`（large raw-TabM 全量重拟），`tap_iron` 逐字节不变。
ZIP SHA-256 `5ed99b8014fb1650b88b6ae57cc3ed4dac378a20831eab5efc800de91ab2c982`，桌面
`round2-V5-time-N0048-Q20-20260926`。**无待测队列**（`round2_current_candidate_queue.submission_priority`
为空，`round2_closure.pending_submissions` 为空）。

**可迁移结论**（证据见 `docs/round2_v5/RESULTS.md`、`docs/round2_v6/RESULTS.md` 与
`EVIDENCE_STATUS.json → round2_closure`）：

1. **四 seed 规则有效。** 在两个从未参与选择的派生 seed 上为正的成员，平台交付 `+0.0409`；旧的
   “两 seed 同号 + 均值 ≥0.005”门槛会放行历史上失败的 N2。晋级 = ≥4 split seed、seed 级配对
   LCB95>0、所有参与 seed 同为正；**折级仅作描述**。
2. **本地幅度被压缩、不能预报平台。** 本地 `+0.00975` → 平台 `+0.0409`（4.19×）；seed-swap 对照
   本地 `−0.0046`、平台 `+0.0005`。本地证据只可用于**方向与排序**（限四 seed 规则筛出的候选）;
   此前“转移误差恒为 0.005”的说法**已撤回**。
3. **边际统计无法识别有用成员。** 十四个见证对象上，残差相关、精度比、行胜率、MAE、混合稳定性
   全都无法把成功与失败排序；只有**平均嵌套增益**可以。真正的绑定判据是**在 incumbent 之上的
   增量混合增益**，不是任何单模型质量统计。
4. **folds 0/1 筛查（1102 行、单 seed）太噪，不能用来排序候选。** 已验证获胜者的单折 α 估计跨
   `0.12–0.38`，随样本才收敛（2 折 0.31、5 折 0.24、10 格 0.20/0.21）——而 V5/V6 的全部选候选
   决策都建立在它之上。**任何后续搜索必须在完整覆盖上筛查。**

**已由实测关闭（无新证据不要重开）：** 混合稳定性签名；在获胜者之上叠加第二个已记录成员（增量
恰好为 0）；铁量容量杠杆（最有利单点过了 ρ 与精度比但不产生混合增益）；**在整套可复现 OOF 库上
的学习式组合器**（27–28 个可复现成员上的嵌套 α 岭回归，相对已交付 incumbent 列只加 `+0.00425`
（6/10 格、其中一个 split seed 为负、低于 `0.0232` MDE），剔除 N-0048 后为 `−0.00139`；相对 V36
看似 `+0.017~+0.023` 大半是 N-0048 已捕获方向的重述，LightGBM 堆叠器两个目标全为负——见
`docs/round2_v6/RESULTS.md` §7）；以及更早关闭的 NODE
逐深度、ODST 核心、TabR 完整融合、残差修正器、样本重加权、密集 α 扫描。时序/滞后特征属**不可用**
（`复赛_train/train_features.csv` 无时间戳或顺序列）。

**堆叠诊断新增的泄漏规则（`docs/round2_v6/RESULTS.md` §7）：** **跨 split seed 平均 OOF 向量是泄漏，
不是方差缩减**——某行在一个 seed 下被留出时，它在另一个 seed 下的分量可能来自**见过该行标签**的模型，
于是留出行的特征本身带标签信息。该缺陷曾造出 `+0.0125/+0.0135` 的假 incumbent 增益，改为**单 seed 内
闭合**后增益消失。这与 V5 复现期的缺陷（拿未混合候选比基座，产生假 `−0.11~−0.14`）属同一错误族。

**若将来重启搜索**，控制性设计是：在**完整覆盖**上筛查、以**增量混合增益**评判、按**四 seed 规则**
晋级、把每次提交当作登记实验。**用户已确认平台取历史最好分**，所以此前"只在平台取最好分时才值得花额度"
的条件已满足：α=0.25 探针**可以安全花掉**，但它是**无预期增益的诊断**（完整覆盖上本地 −0.0005），
不是优化。上文的堆叠关闭与下面的 incumbent 相对成员检查移除了最后的池内希望：**换组合器不属于新问题
类别**，而现有成员对 incumbent 的增量**全部恰好为 0**。

**incumbent 相对成员检查与 α 响应面（2026-09-26，`docs/round2_v6/RESULTS.md` §8，零拟合）：**
在正确参照（已交付 incumbent 列 `0.8×V36+0.2×N-0048`，而非 V36）下，对 **27 条非 v2 完整成员**逐条做
嵌套 2 列混合，**只有 1 条为正**：`v3-mlp-tap_time_len-0000` 为 `+0.00065`（6/10 格）≈ MDE 的 3%；
其余 26 条恰好 `0.00000`（权重被压到 0）或为负。这把 V5 的结论从"相对 V36 无增益"升级为"**现有库中
没有任何成员能在已交付 incumbent 之上留下可测增量**"。完整覆盖的 α 曲线是**干净内点最优**（α=0.20 为峰；
0.25 `−0.0005`、0.30 `−0.0021`、0.40 `−0.0095`、0.60 `−0.0370`），记录的 α=0.25 数字由此升级为 10 格
证据。**D′-2（补全 `raw_mlp|large`/`ple_mlp|large`/`ple_tabm|large`/`raw_tabm|medium`，约 40 折）降级为
先验为负**：它新增的是**同族新配置**，而同族现有完整成员全部恰好为 0，且已记录的 `JM1` 先例
（ρ=0.777 够独立但精度差 → 权重被压为 0）正是新配置面临的处境；`raw_tabm|large` 就是 N-0048 本身。

**平台记分方式（用户回传，2026-09-26）：平台取历史最好分。** 因此新提交**除额度外无下行风险**，是一次
免费期权，**真正独立**候选的组合策略在原则上是合理的。边界要说清楚：仓库内**不存在**这样的候选（除非
新拟合），而近似重复的候选（同基座、成员 ρ0.98）实现高度相关，**不构成分散化**；α 探针安全但只是诊断，
**无预期增益**。

### 分辨率更正（2026-09-26，收尾节之后）

收尾节里"平台对该类扰动的分辨率约 0.0005"**是错的，已撤回**。那个数字是**某一次特定扰动产生的变化**
（seed 平移），不是平台的分辨能力。对已交付包的配对行自举（2000 次，seeds 42/3407，折算到 322 行）：

| 量 | 值 |
|---|---:|
| 配对 Δ 在 2754 行上的 sd | 0.00397 |
| **配对 Δ 在 322 行上的 sd** | **0.0116** |
| 322 行上 95% 最小可分辨差 | **0.0228** |
| 平台实测 Δ（+0.0409） | **3.5σ** |
| 分数**水平**在 322 行上的 sd | 0.119（对 A/B 无关：平台评的是同一批固定行，水平噪声相消） |

战略含义比此前更硬：单列改进**必须 ≥ 约 0.023** 才可能被分辨出来。这解释了 V42 的 `+0.0144` 为何无法转移、
以及所有 `+0.005~0.015` 候选为何读不出来——**唯一有正先验的策略是"大结构性增益"，不是成员微调。**

### 平台确定性与榜单位置（2026-09-26，用户回传）

两条用户信息改变框架，必须随文传递：

- **平台是确定性的。** 重复提交同一个包得分一致，所以**两个包之间的分差是精确的**，没有平台侧抖动可平均。
  上节的配对 `0.0116` 因此**不是平台分辨率**，而是**泛化不确定度**——本地增益在固定 322 行测试集上的实现
  可能偏离多远。本地 `+0.01` 的包，榜单实现大约落在 `+0.01 ± 0.012`；本地 `+0.005` 的包基本是抛硬币。
  已交付的 `+0.0409` 是**精确的**榜单提升，按泛化口径为 3.5σ。
- **榜首 `96.5088`，本项目约在中位数。** 与榜首差 `0.1945`，约为本项目历史最大单次增益（`+0.0409`）的
  **五倍**。而时长列成员混合的经验上限约 `+0.010` 且已被榨取，故**该差距不可能靠继续搜成员弥补**。
  想往上爬必须换成明显更好的模型（不同特征或不同范式）——那是另一个问题类别，先验未知。

因为分数确定性，提交多个**真正独立**的候选是一种组合策略（各自实现固定，取最好者）；但近似重复的候选
（同基座、成员 ρ0.98）实现高度相关，所以只有当模型彼此独立时组合才起作用——这同样属于"新模型"问题，
而不是"搜索"问题。
